# 📊 תרשימי המערכת — מדריך תוכניות

מסמך זה מרכז את התרשימים הנדרשים בהצעת הפרויקט ובספר הפרויקט.
כל התרשימים כתובים בפורמט **Mermaid** ומתרנדרים אוטומטית ב‑GitHub, ב‑VS Code (עם תוסף Mermaid) וב‑Notion.

## תוכן עניינים

1. [תרשים זרימה — הרכזת (הלקוחה הסופית)](#1-תרשים-זרימה--הרכזת)
2. [תרשים זרימה — המפיקה (הלקוחה המשלמת)](#2-תרשים-זרימה--המפיקה)
3. [תרשים מקרי שימוש (Use Case)](#3-תרשים-מקרי-שימוש-use-case)
4. [תרשים ישויות וקשרים (ERD)](#4-תרשים-ישויות-וקשרים-erd)
5. [תרשים ארכיטקטורה (Client / Server / Database)](#5-תרשים-ארכיטקטורה)
6. [תרשים זרימת נתונים (DFD)](#6-תרשים-זרימת-נתונים-dfd)

---

## 1. תרשים זרימה — הרכזת

**מה התרשים מתאר:** את המסע המלא של הרכזת — מהכניסה לאתר, דרך החיפוש והסינון באלפון,
ועד ליצירת הקשר עם המפיקה או שמירת התוכנית באזור האישי.
הרכזת יכולה לגלוש ולפנות גם **ללא הרשמה**; ההרשמה נדרשת רק לשמירת תוכניות ולמעקב.

```mermaid
flowchart TD
    A(["כניסה לאתר — עמוד הבית"]) --> B["מעבר לאלפון התוכניות<br/>/programs"]
    B --> C["טעינת התוכניות מהשרת<br/>GET /api/programs"]
    C --> D["חיפוש וסינון:<br/>קטגוריה · גיל · מיקום · קהל יעד · מחיר"]
    D --> E{"נמצאו תוצאות<br/>מתאימות?"}
    E -->|"לא"| D
    E -->|"כן"| F["לחיצה על כרטיס תוכנית"]
    F --> G["פתיחת חלון פרטי התוכנית<br/>POST /api/programs/:id/view — מונה צפיות"]
    G --> H{"מה הרכזת<br/>בוחרת לעשות?"}

    H -->|"שמירה לאזור האישי"| I{"מחוברת<br/>כרכזת?"}
    I -->|"לא"| J["הרשמה / התחברות<br/>role = coordinator"]
    J --> I
    I -->|"כן"| K["POST /api/coordinator/saved<br/>שמירה או הסרה"]
    K --> L["האזור האישי /coordinator"]
    L --> M["הוספת הערה אישית<br/>+ סימון סטטוס מעקב"]
    M --> N["חישוב תקציב אוטומטי לאירוע"]
    N --> Z

    H -->|"יצירת קשר ישירה"| O["חיוג או מייל<br/>לפי פרטי הקשר בתוכנית"]
    O --> Z

    H -->|"שליחת פנייה דרך האתר"| P["מילוי טופס פנייה:<br/>שם · טלפון · מוסד · הודעה"]
    P --> Q["POST /api/inquiries<br/>ולידציה וניקוי קלט"]
    Q --> R{"הנתונים תקינים<br/>והתוכנית מאושרת?"}
    R -->|"לא — הודעת שגיאה"| P
    R -->|"כן"| S["שמירת הפנייה במסד<br/>status = new"]
    S --> T["שליחת מייל למפיקה ברקע<br/>after — ללא עיכוב הרכזת"]
    T --> U["הודעת אישור לרכזת"]
    U --> Z(["המפיקה חוזרת אל הרכזת<br/>וסוגרת את ההזמנה"])
```

> **הערה לתרשים:** ההפרדה בין "יצירת קשר ישירה" ל"שליחת פנייה דרך האתר" היא מכוונת —
> הפנייה דרך האתר נשמרת במסד ומאפשרת למפיקה מעקב וסטטיסטיקות, בעוד חיוג ישיר לא מתועד.

---

## 2. תרשים זרימה — המפיקה

**מה התרשים מתאר:** את מסלול המפיקה — מההרשמה ופתיחת המנוי, דרך העלאת תוכנית,
בקרת האיכות של המנהלת, ועד לקבלת פניות וצפייה בסטטיסטיקות.

```mermaid
flowchart TD
    A(["מפיקה נכנסת לאתר"]) --> B{"יש לה<br/>חשבון?"}
    B -->|"לא"| C["הרשמה /register<br/>role = producer"]
    C --> D["הצפנת סיסמה bcrypt<br/>+ יצירת מנוי אוטומטי לשנה"]
    D --> E["התחברות אוטומטית<br/>הנפקת JWT ל‑7 ימים"]
    B -->|"כן"| F["התחברות /login<br/>POST /api/auth"]
    F --> G{"אימייל וסיסמה<br/>תקינים?"}
    G -->|"לא"| F
    G -->|"כן"| E
    E --> H["דשבורד המפיקה /dashboard"]

    H --> I{"מה המפיקה<br/>רוצה לעשות?"}

    I -->|"הוספת / עריכת תוכנית"| J["מילוי טופס התוכנית:<br/>תיאור · גילאים · מיקום · מחיר · מדיה"]
    J --> K["POST / PUT /api/dashboard<br/>ולידציית שרת"]
    K --> L{"הנתונים<br/>תקינים?"}
    L -->|"לא"| J
    L -->|"כן"| M["שמירה במסד<br/>status = pending"]
    M --> N["מייל: התוכנית התקבלה<br/>וממתינה לאישור"]
    N --> O["המנהלת בודקת בפאנל /admin"]
    O --> P{"החלטת<br/>המנהלת"}
    P -->|"נדחתה"| Q["מייל דחייה<br/>status = rejected"]
    Q --> J
    P -->|"אושרה"| R["מייל אישור<br/>status = approved"]
    R --> S["התוכנית מוצגת לרכזות באלפון"]
    S --> T["רכזת שולחת פנייה"]
    T --> U["מייל התראה + רשומה במסד"]
    U --> V["עמוד הפניות שלי<br/>/dashboard/inquiries"]
    V --> W["סימון סטטוס:<br/>חדשה / נקראה / טופלה"]
    W --> X(["סגירת עסקה"])

    I -->|"מעקב ביצועים"| Y["GET /api/dashboard/stats<br/>צפיות · פניות · שמירות"]
    Y --> H
    I -->|"ניהול מנוי"| AA["בדיקת תוקף המנוי<br/>ותאריך החידוש"]
    AA --> H
```

---

## 3. תרשים מקרי שימוש (Use Case)

**מה התרשים מתאר:** מי משתמשי המערכת ואילו פעולות כל אחד רשאי לבצע.
שימי לב לחלוקה: אורחת מקבלת גישה לצפייה ופנייה בלבד, ואילו שמירה ומעקב דורשים חשבון רכזת.

```mermaid
flowchart LR
    Guest(["👤 אורחת<br/>ללא הרשמה"])
    Coord(["👩‍🏫 רכזת<br/>משתמשת רשומה"])
    Prod(["👩‍💼 מפיקה<br/>בעלת מנוי"])
    Admin(["🛡️ מנהלת<br/>Admin"])
    Mail(["📧 שירות דואר<br/>SMTP — שחקן חיצוני"])

    subgraph SYS["מערכת מדריך תוכניות"]
        UC1("צפייה באלפון התוכניות")
        UC2("חיפוש וסינון מתקדם")
        UC3("צפייה בפרטי תוכנית")
        UC4("שליחת פנייה למפיקה")
        UC5("הרשמה והתחברות")
        UC6("שמירת תוכנית לאזור אישי")
        UC7("ניהול הערות וסטטוס מעקב")
        UC8("חישוב תקציב לאירוע")
        UC9("הוספת ועריכת תוכנית")
        UC10("ניהול פניות שהתקבלו")
        UC11("צפייה בסטטיסטיקות")
        UC12("ניהול מנוי")
        UC13("אישור או דחיית תוכניות")
        UC14("מחיקת תוכנית מהמערכת")
        UC15("צפייה בכלל המשתמשות והתוכניות")
    end

    Guest --- UC1
    Guest --- UC2
    Guest --- UC3
    Guest --- UC4
    Guest --- UC5

    Coord --- UC1
    Coord --- UC2
    Coord --- UC3
    Coord --- UC4
    Coord --- UC6
    Coord --- UC7
    Coord --- UC8

    Prod --- UC5
    Prod --- UC9
    Prod --- UC10
    Prod --- UC11
    Prod --- UC12

    Admin --- UC13
    Admin --- UC14
    Admin --- UC15

    UC4 -.->|"מפעיל"| Mail
    UC9 -.->|"מפעיל"| Mail
    UC13 -.->|"מפעיל"| Mail
```

**מקרי שימוש מרכזיים — תיאור מילולי:**

| # | מקרה שימוש | שחקן | תנאי קדם | תוצאה |
|---|-----------|------|----------|-------|
| UC4 | שליחת פנייה למפיקה | אורחת / רכזת | התוכנית במצב `approved` | רשומת `Inquiry` נוצרת + מייל למפיקה |
| UC6 | שמירת תוכנית | רכזת מחוברת | `role = coordinator` | רשומת `SavedProgram` ייחודית לזוג רכזת‑תוכנית |
| UC9 | הוספת תוכנית | מפיקה מחוברת | מנוי פעיל | תוכנית נשמרת במצב `pending` |
| UC13 | אישור תוכנית | מנהלת | `role = admin` | `status → approved` + מייל למפיקה |

---

## 4. תרשים ישויות וקשרים (ERD)

**מה התרשים מתאר:** מבנה מסד הנתונים (MongoDB דרך Prisma) — חמש ישויות והקשרים ביניהן.
שימי לב ש‑`Producer` היא טבלת משתמשים אחת המשרתת שלושה תפקידים באמצעות השדה `role`.

```mermaid
erDiagram
    PRODUCER ||--o| SUBSCRIPTION : "בעלת מנוי"
    PRODUCER ||--o{ PROGRAM : "מעלה"
    PRODUCER ||--o{ SAVED_PROGRAM : "שומרת כרכזת"
    PRODUCER ||--o{ INQUIRY : "שולחת כרכזת"
    PRODUCER ||--o{ INQUIRY : "מקבלת כמפיקה"
    PROGRAM  ||--o{ SAVED_PROGRAM : "נשמרת ב"
    PROGRAM  ||--o{ INQUIRY : "מקבלת פניות"

    PRODUCER {
        ObjectId id PK
        string   name
        string   email UK
        string   phone
        string   institution "מוסד — לרכזת בלבד"
        string   passwordHash "bcrypt"
        string   role "producer / coordinator / admin"
        DateTime createdAt
    }

    PROGRAM {
        ObjectId id PK
        string   title
        string   description
        string   category
        int      minAge
        int      maxAge
        string   duration
        string   location
        float    price
        string   phone "פרטי קשר לתוכנית"
        string   email "פרטי קשר לתוכנית"
        string   tags "מערך"
        string   images "מערך Cloudinary"
        string   videos "מערך Cloudinary"
        enum     audience "MEN / WOMEN / BOTH"
        string   status "pending / approved / rejected"
        int      views "מונה צפיות"
        ObjectId producerId FK
        DateTime createdAt
        DateTime updatedAt
    }

    SUBSCRIPTION {
        ObjectId id PK
        DateTime startDate
        DateTime expiryDate
        string   status "active / expired / paused"
        boolean  reminderSent
        ObjectId producerId FK-UK
    }

    SAVED_PROGRAM {
        ObjectId id PK
        string   note "הערה אישית של הרכזת"
        string   trackStatus "saved / contacted / closed / irrelevant"
        ObjectId coordinatorId FK
        ObjectId programId FK
        DateTime createdAt
        DateTime updatedAt
    }

    INQUIRY {
        ObjectId id PK
        string   message
        string   status "new / read / closed"
        string   contactName
        string   contactPhone
        string   contactEmail
        string   contactInstitution
        ObjectId coordinatorId FK "null אם אנונימית"
        ObjectId programId FK
        ObjectId producerId FK
        DateTime createdAt
    }
```

**החלטות תכנון מרכזיות:**

- **טבלת משתמשים אחת** — `Producer` משמשת מפיקה, רכזת ומנהלת. ההבחנה נעשית בשדה `role`, מה שמפשט את מנגנון האימות (JWT יחיד) ומונע כפילות קוד.
- **אילוץ ייחודיות** — `SavedProgram` מוגדרת עם `@@unique([coordinatorId, programId])` כדי למנוע שמירה כפולה של אותה תוכנית.
- **פנייה אנונימית** — `Inquiry.coordinatorId` הוא אופציונלי (`onDelete: SetNull`), כך שגם אורחת שאינה רשומה יכולה לפנות, והפנייה נשמרת גם אם החשבון נמחק.
- **מחיקה מדורגת** — מחיקת תוכנית מוחקת אוטומטית את השמירות והפניות הקשורות אליה (`onDelete: Cascade`).

---

## 5. תרשים ארכיטקטורה

**מה התרשים מתאר:** את שכבות המערכת ואת זרימת הבקשה מהדפדפן ועד למסד הנתונים,
כולל שכבת האבטחה והשירותים החיצוניים.

```mermaid
flowchart TB
    subgraph CLIENT["🖥️ צד לקוח — Client"]
        direction LR
        C1["דפדפן<br/>React 19 + Next.js 16"]
        C2["עמודי המערכת:<br/>/programs · /dashboard<br/>/coordinator · /admin"]
        C3["localStorage<br/>JWT לפי תפקיד"]
        C1 --- C2 --- C3
    end

    subgraph MW["🛡️ שכבת אבטחה — Middleware"]
        direction LR
        M1["Rate Limiting<br/>100 בקשות לדקה"]
        M2["הגנת CSRF<br/>בדיקת Origin"]
        M3["Security Headers<br/>X-Frame-Options ועוד"]
    end

    subgraph SERVER["⚙️ צד שרת — Next.js API Routes"]
        direction TB
        S1["/api/auth · /api/register<br/>אימות והרשמה"]
        S2["/api/programs<br/>אלפון ציבורי"]
        S3["/api/dashboard · /stats<br/>ניהול תוכניות וסטטיסטיקות"]
        S4["/api/coordinator/saved<br/>אזור אישי לרכזת"]
        S5["/api/inquiries<br/>פניות"]
        S6["/api/admin/programs<br/>בקרת איכות"]
        LIB["שכבת לוגיקה משותפת:<br/>auth · api-auth · validation · sanitize"]
        S1 & S2 & S3 & S4 & S5 & S6 --- LIB
    end

    subgraph DATA["💾 נתונים ושירותים חיצוניים"]
        direction LR
        D1[("MongoDB Atlas<br/>דרך Prisma ORM 6")]
        D2["☁️ Cloudinary<br/>תמונות וסרטונים"]
        D3["📧 SMTP / Nodemailer<br/>מיילים אוטומטיים"]
    end

    CLIENT -->|"HTTPS · fetch עם Bearer Token"| MW
    MW -->|"בקשה שעברה סינון"| SERVER
    SERVER -->|"תשובת JSON"| CLIENT
    LIB -->|"שאילתות Prisma"| D1
    CLIENT -.->|"העלאה ישירה"| D2
    LIB -.->|"התראות ברקע"| D3
```

**זרימת בקשה טיפוסית (דוגמה — שליחת פנייה):**

```
דפדפן → POST /api/inquiries
      → Middleware: בדיקת Rate Limit + Origin
      → Route: ולידציה ב‑Zod + סניטציה
      → Prisma: אימות שהתוכנית מאושרת → יצירת Inquiry
      → תשובת JSON חוזרת מיד ללקוח
      → after(): שליחת מייל למפיקה ברקע
```

---

## 6. תרשים זרימת נתונים (DFD)

**מה התרשים מתאר:** את תנועת המידע בין הישויות החיצוניות, התהליכים המרכזיים ומאגרי הנתונים.
זהו תרשים ברמה 1 — פירוט התהליכים העיקריים של המערכת.

```mermaid
flowchart TD
    EXT_C(["רכזת / אורחת"])
    EXT_P(["מפיקה"])
    EXT_A(["מנהלת"])
    EXT_M(["שירות דואר SMTP"])

    P1["1.0<br/>אימות והרשאות"]
    P2["2.0<br/>חיפוש וסינון תוכניות"]
    P3["3.0<br/>ניהול תוכניות"]
    P4["4.0<br/>בקרת איכות ואישור"]
    P5["5.0<br/>ניהול פניות"]
    P6["6.0<br/>אזור אישי ומעקב"]
    P7["7.0<br/>סטטיסטיקות"]

    DS1[("D1 · Producers")]
    DS2[("D2 · Programs")]
    DS3[("D3 · Subscriptions")]
    DS4[("D4 · SavedPrograms")]
    DS5[("D5 · Inquiries")]

    EXT_C -->|"פרטי הרשמה / התחברות"| P1
    EXT_P -->|"פרטי הרשמה / התחברות"| P1
    P1 -->|"קריאה וכתיבת משתמשות"| DS1
    P1 -->|"יצירת מנוי"| DS3
    P1 -->|"טוקן JWT"| EXT_C
    P1 -->|"טוקן JWT"| EXT_P

    EXT_C -->|"קריטריוני חיפוש"| P2
    P2 -->|"שליפת תוכניות מאושרות"| DS2
    P2 -->|"רשימת תוצאות"| EXT_C

    EXT_P -->|"פרטי תוכנית חדשה / עדכון"| P3
    P3 -->|"שמירה במצב pending"| DS2
    P3 -->|"בקשת התראה"| EXT_M

    EXT_A -->|"החלטת אישור / דחייה"| P4
    P4 -->|"עדכון status"| DS2
    P4 -->|"בקשת התראה"| EXT_M
    DS2 -->|"תוכניות ממתינות"| P4

    EXT_C -->|"טופס פנייה"| P5
    P5 -->|"שמירת פנייה"| DS5
    P5 -->|"בקשת התראה"| EXT_M
    DS5 -->|"רשימת פניות"| P5
    P5 -->|"הפניות שלי"| EXT_P
    EXT_P -->|"עדכון סטטוס פנייה"| P5

    EXT_C -->|"שמירה · הערה · סטטוס מעקב"| P6
    P6 -->|"כתיבה וקריאה"| DS4
    DS2 -->|"פרטי התוכניות השמורות"| P6
    P6 -->|"רשימה שמורה + סיכום תקציב"| EXT_C

    DS2 -->|"מוני צפיות"| P7
    DS5 -->|"ספירת פניות"| P7
    DS4 -->|"ספירת שמירות"| P7
    P7 -->|"דוח ביצועים"| EXT_P

    EXT_M -->|"מייל למשתמשת"| EXT_P
```

**מקרא:**

| סימון | משמעות |
|-------|---------|
| עיגול מעוגל | ישות חיצונית — משתמש או שירות מחוץ למערכת |
| מלבן ממוספר | תהליך — פעולה שהמערכת מבצעת על הנתונים |
| גליל | מאגר נתונים — אוסף ב‑MongoDB |
| חץ | זרימת נתונים וכיוונה |

---

## נספח — איך לייצא את התרשימים לתמונה

עבור הגשה מודפסת של ספר הפרויקט:

1. **הדרך המהירה** — לפתוח את הקובץ ב‑GitHub או ב‑VS Code, לצלם מסך של התרשים.
2. **ייצוא איכותי** — להעתיק את קוד ה‑Mermaid ל‑[mermaid.live](https://mermaid.live) ולייצא כ‑PNG או SVG.
3. **הדפסה ישירה** — לפתוח את העמוד הוויזואלי המצורף בדפדפן ולהדפיס ל‑PDF.
