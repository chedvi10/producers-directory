# תוכנית מימוש – ערך מוסף למערכת "מדריך תוכניות"

מסמך זה מפרט את המימוש של שלושה פיצ'רים חדשים המוסיפים ערך משמעותי למערכת:

1. **אזור אישי לרכזת** – מועדפים, הערות אישיות, סטטוס מעקב וחישוב תקציב.
2. **מערכת פניות** – רכזת פונה למפיקה דרך האתר, המפיקה מקבלת "לידים".
3. **סטטיסטיקות למפיקה** – צפיות, פניות ושמירות למועדפים לכל תוכנית.

שלושת הפיצ'רים משלימים זה את זה והופכים את האתר מ"אלפון" ל"כלי עבודה" עבור שלושת הצדדים: הרכזת, המפיקה והמנהלת.

---

## 0. החלטה ארכיטקטונית מרכזית – סוג משתמש חדש

כיום קיים מודל משתמש אחד בלבד: `Producer`, עם דגל `isAdmin`. הרכזת שגולשת באלפון היא **אנונימית לחלוטין ואין לה חשבון**. כדי לאפשר אזור אישי ופניות, צריך שהרכזת תוכל להירשם ולהתחבר.

**הגישה המומלצת: הוספת שדה `role` למודל הקיים** (במקום מודל נפרד).
היתרון: שימוש חוזר בכל תשתית ההזדהות שכבר קיימת (JWT, `/login`, `/register`, `validateAuth`), עם מינימום קוד חדש.

| ערך `role` | תיאור |
|------------|-------|
| `producer` | מפיקה (ברירת מחדל, המצב הקיים) |
| `coordinator` | רכזת – חשבון חדש |
| `admin` | מנהלת (מחליף בהדרגה את `isAdmin`) |

> חלופה: מודל `Coordinator` נפרד. נקי יותר סמנטית אבל דורש שכפול של login/register/JWT. לפרויקט גמר – עדיף `role`, וכדאי להזכיר את השיקול הזה בספר הפרויקט.

**שינויים בהזדהות:**
- `lib/auth.ts` → `JWTPayload` יכלול `role: string` (בנוסף ל־`isAdmin` הקיים לתאימות לאחור).
- `/register` יקבל פרמטר `role`. לרכזת **לא** נוצר `Subscription` (זה רק למפיקות), ושדה `phone` יהפוך לאופציונלי עבורה.
- הגנת ניתוב: הפניה לאזור המתאים לפי `role` אחרי התחברות (רכזת → `/coordinator`, מפיקה → `/dashboard`, מנהלת → `/admin`).

---

## 1. שינויים בסכמת הנתונים (`prisma/schema.prisma`)

```prisma
model Producer {
  // ... שדות קיימים ...
  role         String   @default("producer") // producer / coordinator / admin
  savedItems   SavedProgram[]                 // מועדפים/הערות (כרכזת)
  inquiriesSent    Inquiry[] @relation("CoordinatorInquiries") // פניות ששלחה
  inquiriesReceived Inquiry[] @relation("ProducerInquiries")   // פניות שקיבלה
}

model Program {
  // ... שדות קיימים ...
  views         Int      @default(0)  // מונה צפיות (לסטטיסטיקות)
  savedBy       SavedProgram[]
  inquiries     Inquiry[]
}

// מועדפים + הערה אישית + סטטוס מעקב של רכזת על תוכנית
model SavedProgram {
  id            String   @id @default(auto()) @map("_id") @db.ObjectId
  note          String?  // הערה חופשית של הרכזת
  trackStatus   String   @default("saved") // saved / contacted / closed / irrelevant
  coordinatorId String   @db.ObjectId
  coordinator   Producer @relation(fields: [coordinatorId], references: [id])
  programId     String   @db.ObjectId
  program       Program  @relation(fields: [programId], references: [id])
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@unique([coordinatorId, programId]) // תוכנית נשמרת פעם אחת לרכזת
}

// פנייה מרכזת אל מפיקה בנוגע לתוכנית
model Inquiry {
  id            String   @id @default(auto()) @map("_id") @db.ObjectId
  message       String
  status        String   @default("new") // new / read / replied / closed
  contactName   String   // שם ליצירת קשר (גם אם אנונימי)
  contactPhone  String
  contactEmail  String?
  coordinatorId String?  @db.ObjectId   // אופציונלי – מאפשר גם פנייה אנונימית
  coordinator   Producer? @relation("CoordinatorInquiries", fields: [coordinatorId], references: [id])
  programId     String   @db.ObjectId
  program       Program  @relation(fields: [programId], references: [id])
  producerId    String   @db.ObjectId
  producer      Producer @relation("ProducerInquiries", fields: [producerId], references: [id])
  createdAt     DateTime @default(now())
}
```

לאחר השינוי: `npx prisma generate` (ובמונגו אין צורך ב־migrate פורמלי, אך יש להריץ generate ולוודא שהקליינט מתעדכן).

**עדכון טיפוסים** ב־`types/program.ts`: הוספת `SavedProgram`, `Inquiry`, ושדה `role` ל־`Producer`, `views` ל־`Program`.

---

## 2. פיצ'ר א' – אזור אישי לרכזת

### מסכים (App Router)
| נתיב | תיאור |
|------|-------|
| `/app/coordinator/page.tsx` | האזור האישי של הרכזת: רשימת התוכניות השמורות, הערה + סטטוס לכל אחת, וסיכום תקציב. |
| `/app/register/page.tsx` | הוספת בחירה: "אני מפיקה" / "אני רכזת" (משנה את ה־`role` בבקשה). |

### רכיבים
- `components/coordinator/SavedProgramCard.tsx` – כרטיס תוכנית שמורה: כותרת, מחיר, שדה הערה (עריכה מקומית + שמירה), בורר סטטוס מעקב, כפתור הסרה, כפתור "פנייה למפיקה".
- `components/coordinator/BudgetSummary.tsx` – סיכום: מספר תוכניות שמורות + סכום המחירים (שדה `price` כבר קיים). מסנן לפי סטטוס (למשל רק "סגורות").
- כפתור ⭐ **"שמור"** ברכיב `ProgramModal` הקיים (באלפון הציבורי) – מוסיף/מסיר מהמועדפים. מוצג רק לרכזת מחוברת.

### API
`/app/api/coordinator/saved/route.ts`:
- `GET` – כל התוכניות השמורות של הרכזת (`include: { program: true }`).
- `POST` – שמירת תוכנית (`{ programId }`) → יצירת `SavedProgram` (`upsert` על ה־`@@unique`).
- `PUT` – עדכון הערה/סטטוס (`{ savedId, note, trackStatus }`).
- `DELETE` – הסרה (`?savedId=`).

בכל הפעולות: `validateAuth` + בדיקה ש־`role === 'coordinator'` ושהרשומה שייכת לרכזת המחוברת.

---

## 3. פיצ'ר ב' – מערכת פניות

### זרימה
רכזת (מהמודל או מכרטיס תוכנית) → טופס פנייה → נוצרת `Inquiry` → נשלח מייל למפיקה (nodemailer קיים) → המפיקה רואה את הפנייה בדשבורד.

### מסכים ורכיבים
- `components/InquiryForm.tsx` – טופס: שם, טלפון, אימייל (אופציונלי), הודעה. אם הרכזת מחוברת – השדות מתמלאים אוטומטית מהחשבון. נפתח מתוך `ProgramModal` / `SavedProgramCard`.
- `/app/dashboard/inquiries/page.tsx` – "הפניות שלי" למפיקה: רשימת פניות עם סטטוס (חדש/נקרא/טופל), שם התוכנית ופרטי הרכזת ליצירת קשר.
- הוספת קישור "פניות" + מונה פניות חדשות ל־header של הדשבורד (`/app/dashboard/page.tsx`).

### API
`/app/api/inquiries/route.ts`:
- `POST` (פתוח – תומך גם באנונימי): ולידציה ב־zod, יצירת `Inquiry` עם `status:'new'`, שליחת מייל למפיקה.
- `GET` (מוגן, מפיקה): כל הפניות שקיבלה (`where: { producerId }`), ממוין לפי תאריך.
- `PUT` (מוגן, מפיקה): עדכון סטטוס פנייה (`{ inquiryId, status }`), עם אימות בעלות.

### מייל חדש (`lib/email.ts`)
`sendNewInquiryEmail(producerEmail, producerName, programTitle, contact, message)` – בסגנון המיילים הקיימים (RTL, אותה מסגרת עיצובית), עם פרטי הרכזת וההודעה.

---

## 4. פיצ'ר ג' – סטטיסטיקות למפיקה

### איסוף הנתונים
- **צפיות:** הגדלת `Program.views` בכל פתיחת מודל תוכנית. נקודת חיבור: ב־`GET /api/programs` או ב־endpoint ייעודי `POST /api/programs/[id]/view` שנקרא כשנפתח `ProgramModal`. (מומלץ endpoint נפרד כדי לא לספור טעינת רשימה כצפייה.)
- **פניות:** נספר מ־`Inquiry` לפי `programId`.
- **שמירות למועדפים:** נספר מ־`SavedProgram` לפי `programId`.

### API
`/app/api/dashboard/stats/route.ts` (מוגן, מפיקה):
מחזיר לכל תוכנית של המפיקה: `{ programId, title, views, inquiriesCount, savedCount }`, וכן סיכומים כלליים (סה"כ צפיות/פניות). שימוש ב־`groupBy` / `count` של Prisma.

### תצוגה
- `components/dashboard/StatsOverview.tsx` – שורת כרטיסי סיכום בראש הדשבורד: סה"כ צפיות, סה"כ פניות, סה"כ שמירות.
- הצגת המונים גם על כל `ProgramCard` (אייקון עין + מספר, אייקון פנייה + מספר).

---

## 5. סדר עבודה מומלץ (Phases)

**שלב 1 – תשתית משתמש רכזת** (בסיס לכל השאר)
1. הוספת `role` לסכמה + `prisma generate`.
2. עדכון `JWTPayload`, `/register` (בחירת סוג), הפניה לפי role אחרי login.
3. עדכון `types/program.ts`.

**שלב 2 – אזור אישי לרכזת**
4. מודל `SavedProgram` + API `coordinator/saved`.
5. כפתור ⭐ ב־`ProgramModal`, עמוד `/coordinator`, רכיבי כרטיס ותקציב.

**שלב 3 – מערכת פניות**
6. מודל `Inquiry` + API `inquiries` + מייל.
7. `InquiryForm`, עמוד `/dashboard/inquiries`, מונה ב־header.

**שלב 4 – סטטיסטיקות**
8. `views` + endpoint צפייה, API `dashboard/stats`.
9. `StatsOverview` + מונים על הכרטיסים.

**שלב 5 – ליטוש ובדיקות**
10. בדיקות Jest ל־API החדש (בדומה לקיים ב־`_test_`).
11. עדכון `README.md` ו־`ספר-פרויקט.md` בתיאור הפיצ'רים.

---

## 6. סיכום קבצים

| פעולה | קובץ |
|-------|------|
| שינוי | `prisma/schema.prisma`, `types/program.ts`, `lib/auth.ts`, `lib/email.ts` |
| שינוי | `app/register/page.tsx`, `app/dashboard/page.tsx`, `components/ProgramModal.tsx` |
| חדש – רכזת | `app/coordinator/page.tsx`, `app/api/coordinator/saved/route.ts`, `components/coordinator/SavedProgramCard.tsx`, `components/coordinator/BudgetSummary.tsx` |
| חדש – פניות | `app/api/inquiries/route.ts`, `app/dashboard/inquiries/page.tsx`, `components/InquiryForm.tsx` |
| חדש – סטטיסטיקות | `app/api/dashboard/stats/route.ts`, `app/api/programs/[id]/view/route.ts`, `components/dashboard/StatsOverview.tsx` |

---

## 7. נקודות לתשומת לב

- **הגנה בצד שרת:** הניתוב כיום נאכף בצד לקוח בלבד (`middleware.ts` רק מוסיף headers). בכל endpoint חדש חובה `validateAuth` + בדיקת `role`/בעלות, בדיוק כפי שנעשה ב־`api/admin`.
- **פניות אנונימיות:** ה־`Inquiry` תומך גם ברכזת לא־מחוברת (`coordinatorId` אופציונלי). אפשר להשאיר פתוח לערך מוסף מיידי, או לחייב התחברות – החלטה מוצרית.
- **מניעת ספירה כפולה של צפיות:** אפשר להגביל צפייה למפיקה עצמה (לא לספור צפיות של בעל התוכנית) ו/או להשתמש ב־debounce בצד לקוח.
- **שימוש חוזר:** לשמר את שפת העיצוב הקיימת (גרדיאנט סגול/ורוד, RTL, `lucide-react`) בכל הרכיבים החדשים.
