<div dir="rtl">

<div align="center">

# 📚 מדריך תוכניות
### אלפון דיגיטלי לתוכניות, מרצים והפקות לאירועים

---

**ספר פרויקט גמר**

מוגש למכון הממשלתי להכשרה בטכנולוגיה ובמדע (מה"ט)

---

| | |
|---|---|
| **שם הפרויקט** | מדריך תוכניות (Producers Directory) |
| **שם המגישה** | ⟪שם מלא⟫ |
| **שם המנחה** | ⟪שם המנחה⟫ |
| **המוסד הלימודי** | ⟪שם המכללה / המוסד⟫ |
| **מסלול / מגמה** | ⟪הנדסאי תוכנה / מגמה⟫ |
| **שנת הגשה** | 2026 |

</div>

<div style="page-break-after: always;"></div>

---

## תודות

ברצוני להודות לכל מי שליווה ותמך בי לאורך הדרך בהקמת פרויקט זה:

- **למנחה הפרויקט, ⟪שם המנחה⟫** — על ההכוונה המקצועית, הסבלנות והליווי הצמוד לאורך כל שלבי הפיתוח.
- **לצוות המורים והמכללה ⟪שם המוסד⟫** — על הידע, הכלים והתשתית שאפשרו לי להגיע עד לכאן.
- **למשפחתי ולחבריי** — על העידוד, התמיכה והאמונה בי גם ברגעים המאתגרים.
- **למפיקות והרכזות** ששיתפו עמי את הצרכים האמיתיים מהשטח, ועל בסיסם נבנתה המערכת.

תודה לכולם — הפרויקט הזה הוא תוצר של תמיכה משותפת.

<div style="page-break-after: always;"></div>

---

## הקדמה / תקציר

**מדריך תוכניות** הוא פלטפורמת ווב המשמשת כאלפון דיגיטלי מרכזי לעולם התוכניות, ההרצאות וההפקות לאירועים. המערכת מחברת בין שני קהלים: **מפיקות ובעלות עסקים** המעוניינות לחשוף את התוכניות שלהן לקהל רחב, ובין **רכזות ומחפשות תוכניות** הזקוקות לדרך מהירה ונוחה למצוא את התוכנית המתאימה לאירוע. לרכזות מוצע גם **חשבון אישי** (ללא עלות) המאפשר לשמור תוכניות מהאלפון, לנהל הערות ומעקב, לחשב תקציב לאירוע ולשלוח פניות ישירות למפיקות דרך האתר.

עד היום, איתור תוכנית לאירוע נעשה דרך פניות אישיות, המלצות מפה לאוזן וחיפושים מפוזרים — תהליך איטי, חלקי ולא יעיל. המערכת פותרת זאת באמצעות אלפון אחד, מסונן וניתן לחיפוש, שבו כל תוכנית מוצגת עם כל פרטיה (תיאור, גיל מטרה, מיקום, מחיר, מדיה ופרטי יצירת קשר).

המערכת בנויה בארכיטקטורת **Full-Stack מודרנית** מבוססת **Next.js 16** ו‑**React 19** בצד הלקוח, **API Routes** ו‑**Prisma ORM** בצד השרת, ובסיס נתונים **MongoDB**. המערכת כוללת מנגנון **הרשמה והתחברות מאובטח** (JWT + הצפנת סיסמאות) עם **שלושה תפקידים** (מפיקה / רכזת / מנהלת), **דשבורד ניהול אישי** למפיקות הכולל **סטטיסטיקות** (צפיות, פניות ושמירות לכל תוכנית), **אזור אישי לרכזת** (תוכניות שמורות, הערות, מעקב סטטוס וחישוב תקציב), **מערכת פניות** מהאתר אל המפיקות עם התראה במייל, **מנגנון אישור תוכניות** על‑ידי מנהלת, **העלאת תמונות וסרטונים** דרך Cloudinary, **מערכת התראות במייל**, וכן שכבות אבטחה (Rate Limiting, הגנת CSRF, אימות נתונים).

מסמך זה מתאר את הרעיון, אופן פעולת המערכת, מבנה הנתונים, הלוגיקה המרכזית, הארכיטקטורה, מדריך למשתמש לפי תפקידים, תיאור המסכים, וסיכום הלמידה והקשיים.

<div style="page-break-after: always;"></div>

---

## הצעת הפרויקט

> *פרק זה משקף את הצעת הפרויקט כפי שגובשה בתחילת הדרך.*

### רקע וצורך
בשוק האירועים קיים פער בין **מפיקות התוכניות** לבין **הרכזות** המארגנות אירועים. למפיקות קשה להגיע לקהל יעד ממוקד, ולרכזות קשה לאתר במהירות תוכנית מתאימה לפי קטגוריה, גיל, מיקום ותקציב. אין כיום פלטפורמה דיגיטלית מרכזית אחת שמרכזת את ההיצע באופן מסודר ומסונן.

### מטרת הפרויקט
פיתוח פלטפורמת ווב המהווה **אלפון דיגיטלי** המאפשר:
- למפיקות — להעלות ולנהל את התוכניות שלהן בעצמן.
- לרכזות — לחפש, לסנן ולמצוא תוכניות, ולפנות ישירות למפיקה.
- למנהלת המערכת — לפקח על איכות התוכן באמצעות אישור תוכניות לפני פרסומן.

### היקף ראשוני (Scope)
1. מערכת הרשמה והתחברות למפיקות.
2. דשבורד אישי לניהול תוכניות (הוספה, עריכה, מחיקה).
3. אלפון תוכניות ציבורי עם חיפוש וסינון.
4. העלאת מדיה (תמונת פרסום וסרטון טריילר).
5. מנגנון אישור/דחיית תוכניות על‑ידי מנהלת.
6. התראות במייל בשלבי התהליך.
7. מנגנון מנוי שנתי למפיקות.

### הרחבות שנוספו במהלך הפיתוח
במהלך העבודה זוהו צרכים נוספים מהשטח, והיקף הפרויקט הורחב:
1. **חשבון רכזת** — הרשמה ייעודית לרכזות (ללא מנוי), הכוללת שם מוסד.
2. **אזור אישי לרכזת** — שמירת תוכניות מהאלפון (⭐), הערה אישית לכל תוכנית, מעקב סטטוס (שמורה / יצרתי קשר / סגרתי / לא רלוונטי) וחישוב תקציב אוטומטי לאירוע.
3. **מערכת פניות** — טופס פנייה למפיקה ישירות מכרטיס התוכנית (גם לגולשת אנונימית), התראה במייל למפיקה, ועמוד "הפניות שלי" לניהול הפניות.
4. **סטטיסטיקות למפיקה** — מונה צפיות, פניות ושמירות לכל תוכנית.
5. **סינון מתקדם** — סינון לפי גיל מדויק (טווח גילאים) ולפי קהל יעד (בנים / בנות / כולם).

### תוצרים צפויים
מערכת עובדת מקצה לקצה, מאובטחת ורספונסיבית, התומכת בעברית מלאה (RTL), הניתנת להרצה מקומית ולפריסה בענן.

<div style="page-break-after: always;"></div>

---

## תוכן עניינים

1. [מבוא](#מבוא)
2. [פירוט על הפרויקט – איך המערכת עובדת](#פירוט-על-הפרויקט--איך-המערכת-עובדת)
3. [מבנה נתונים](#מבנה-נתונים)
4. [פונקציות ולוגיקה מרכזית](#פונקציות-ולוגיקה-מרכזית)
5. [מבנה המערכת (Client / Server / Database)](#מבנה-המערכת-client--server--database)
6. [מדריך משתמש לפי סוגי משתמשים](#מדריך-משתמש-לפי-סוגי-משתמשים)
7. [תיאור מסכים](#תיאור-מסכים)
8. [סיכום](#סיכום)

<div style="page-break-after: always;"></div>

---

## מבוא

### הרעיון
**מדריך תוכניות** הוא אלפון דיגיטלי חכם המרכז במקום אחד את כל התוכניות, ההרצאות, האטרקציות והשירותים לאירועים. הרעיון נולד מתוך צורך אמיתי בשטח: מצד אחד מפיקות מוכשרות שמתקשות לשווק את עצמן, ומצד שני רכזות שמבזבזות זמן יקר בחיפוש אחר תוכנית מתאימה.

המערכת יוצרת "שוק" מסודר: המפיקה מעלה את התוכנית שלה פעם אחת, והיא הופכת זמינה וניתנת לחיפוש עבור כל הרכזות, 24/7, מכל מכשיר.

### הבעיה שהמערכת פותרת
- **פיזור מידע** — ההיצע מפוזר בין קבוצות, המלצות ופרסומות, ללא מקור אחד אמין.
- **חיפוש לא יעיל** — אין דרך לסנן לפי קטגוריה, גיל יעד, מיקום ומחיר בו‑זמנית.
- **חשיפה מוגבלת למפיקות** — קשה להגיע לקהל יעד רלוונטי.
- **חוסר בקרת איכות** — אין גורם שמוודא שהתוכן המוצג רלוונטי ותקין.

### הפתרון
אלפון דיגיטלי המבוסס על ארבעה עקרונות:
1. **עצמאות למפיקה** — ניהול עצמי מלא של התוכניות דרך דשבורד אישי, כולל סטטיסטיקות ופניות.
2. **חוויית חיפוש לרכזת** — סינון מהיר וצפייה בפרטים מלאים במסך אחד, ללא צורך בהרשמה.
3. **אזור עבודה אישי לרכזת** — רכזת רשומה יכולה לשמור תוכניות, לנהל הערות ומעקב, לחשב תקציב ולפנות למפיקות — הכול במקום אחד.
4. **בקרת איכות** — כל תוכנית עוברת אישור מנהלת לפני שהיא מתפרסמת באלפון.

<div style="page-break-after: always;"></div>

---

## פירוט על הפרויקט – איך המערכת עובדת

המערכת מבוססת על ארבעה סוגי משתמשים: **גולשת אורחת**, **רכזת רשומה**, **מפיקה**, ו**מנהלת**. ההבחנה בין המשתמשים הרשומים נעשית באמצעות שדה `role` (תפקיד): `producer` / `coordinator` / `admin`. לכל תפקיד תהליך עבודה משלו.

### תהליך המפיקה (מהרשמה ועד פרסום)
1. המפיקה נכנסת לעמוד הבית ולוחצת "הצטרפי כמפיקה".
2. בטופס ה**הרשמה** היא בוחרת בתפקיד "מפיקה" וממלאת שם, אימייל, טלפון וסיסמה (+ אימות סיסמה). בעת ההרשמה נוצר עבורה אוטומטית **מנוי פעיל לשנה**, והיא מחוברת מיידית (ללא צורך בהתחברות נוספת).
3. לאחר ההרשמה היא מועברת ל**דשבורד האישי**, שבו מוצגים מצב המנוי, **סטטיסטיקות** (צפיות, פניות ושמירות) ורשימת התוכניות שלה.
4. בלחיצה על "הוסף תוכנית" נפתח טופס. שדות הטלפון והאימייל מתמלאים **אוטומטית** מפרטי המפיקה (וניתנים לשינוי לכל תוכנית).
5. המפיקה ממלאת את פרטי התוכנית — כולל **טווח גילאים** (מגיל–עד גיל) ו**קהל יעד** (בנים / בנות / כולם) — מעלה **תמונת פרסום** ו**סרטון טריילר** (אופציונלי) דרך Cloudinary, ושומרת.
6. התוכנית נשמרת בסטטוס **`pending`** (ממתינה לאישור), ונשלח למפיקה **מייל אישור קליטה**.
7. כל עוד התוכנית לא אושרה — היא אינה מופיעה באלפון הציבורי.
8. כשרכזת שולחת פנייה על תוכנית, המפיקה מקבלת **מייל התראה** ויכולה לנהל את הפנייה בעמוד **"הפניות שלי"** (סימון: חדשה / נקראה / טופלה).

### תהליך המנהלת (בקרת איכות)
1. המנהלת מתחברת עם משתמש בעל תפקיד `admin`, ומופנית אוטומטית ל**פאנל הניהול** (`/admin`).
2. בפאנל מוצגות **כל** התוכניות במערכת עם פרטי המפיקה, וניתן לסנן לפי סטטוס: הכל / ממתין / מאושר / נדחה (עם מונה לכל סטטוס).
3. המנהלת בוחנת תוכנית ומחליטה: **אישור**, **דחייה** או **מחיקה**.
4. בעת אישור/דחייה נשלח למפיקה **מייל מתאים** (אושר / נדחה).
5. רק תוכנית בסטטוס **`approved`** מתפרסמת באלפון לרכזות.

### תהליך הרכזת (חיפוש, שמירה ויצירת קשר)
1. הרכזת נכנסת לעמוד "אלפון התוכניות" (`/programs`) — הצפייה והחיפוש פתוחים **גם ללא הרשמה**.
2. היא מסננת לפי **חיפוש חופשי** (כותרת/תיאור), **קטגוריה**, **גיל** (המערכת מאתרת תוכניות שטווח הגילאים שלהן כולל את הגיל שהוזן), **קהל יעד**, **מיקום** (חיפוש חלקי) ו**מחיר מקסימלי**.
3. הרשימה מתעדכנת בזמן אמת מול השרת בכל שינוי פילטר.
4. בלחיצה על תוכנית נפתח **חלון פרטים מלא (Modal)**: תמונות, סרטון, תיאור, גילאים, קהל יעד, מיקום, משך, מחיר ו**פרטי יצירת קשר** (טלפון/אימייל). כל פתיחה של החלון מגדילה את **מונה הצפיות** של התוכנית.
5. הרכזת יכולה **לשלוח פנייה למפיקה ישירות מהאתר** — טופס קצר (שם, טלפון, אימייל, מוסד, הודעה) הפתוח גם לגולשת אנונימית. המפיקה מקבלת את הפנייה במייל ובעמוד הפניות שלה.
6. רכזת שנרשמה לחשבון רכזת (הרשמה ייעודית ללא מנוי, הכוללת שם מוסד) מקבלת **אזור אישי** (`/coordinator`) ויכולה בנוסף:
   - **לשמור תוכניות** מהאלפון בלחיצת ⭐ (ולהסיר בלחיצה חוזרת).
   - לכתוב **הערה אישית** על כל תוכנית שמורה (למשל: "דיברתי איתה, פנויה רק בחנוכה").
   - לסמן **סטטוס מעקב**: שמורה / יצרתי קשר / סגרתי / לא רלוונטי.
   - לראות **סיכום תקציב** אוטומטי: עלות כוללת של התוכניות הרלוונטיות ותקציב "סגור" של תוכניות שסומנו "סגרתי".
   - לשלוח פנייה למפיקה גם מתוך כרטיס התוכנית השמורה, כשפרטיה מולאו מראש מהפרופיל.

### מה כל משתמש יכול לעשות (תמצית)
| יכולת | אורחת | רכזת רשומה | מפיקה | מנהלת |
|---|:---:|:---:|:---:|:---:|
| צפייה באלפון התוכניות המאושרות | ✔ | ✔ | ✔ | ✔ |
| חיפוש וסינון | ✔ | ✔ | ✔ | ✔ |
| שליחת פנייה למפיקה | ✔ | ✔ | – | – |
| הרשמה / התחברות | – | ✔ | ✔ | ✔ |
| שמירת תוכניות + הערות + מעקב + תקציב | – | ✔ | – | – |
| הוספת / עריכת / מחיקת תוכנית אישית | – | – | ✔ | – |
| צפייה בסטטיסטיקות ובפניות שהתקבלו | – | – | ✔ | – |
| צפייה במצב מנוי אישי | – | – | ✔ | – |
| צפייה בכל התוכניות + פרטי מפיקות | – | – | – | ✔ |
| אישור / דחיית / מחיקת תוכנית | – | – | – | ✔ |

<div style="page-break-after: always;"></div>

---

## מבנה נתונים

בסיס הנתונים הוא **MongoDB**, מנוהל באמצעות **Prisma ORM**. המודל כולל חמש ישויות (אוספים): `Producer`, `Program`, `Subscription`, `SavedProgram`, `Inquiry`, וכן טיפוס מנוי (enum) בשם `Audience` לקהל היעד.

### תרשים קשרים (ERD לוגי)

```
┌──────────────┐  1 : 1   ┌─────────────────────┐  1 : N   ┌──────────────┐
│ Subscription │──────────│      Producer       │──────────│   Program    │
│    (מנוי)     │          │  (משתמש: מפיקה /     │          │   (תוכנית)    │
└──────────────┘          │   רכזת / מנהלת)      │          └──────────────┘
                          └─────────────────────┘

Producer (רכזת)  ── 1 : N ──►  SavedProgram  ◄── N : 1 ──  Program
                               (תוכנית שמורה: הערה + סטטוס מעקב)

Producer (רכזת)  ── 1 : N ──►  Inquiry       ◄── N : 1 ──  Program
Producer (מפיקה) ── 1 : N ──►  (פנייה: הודעה + פרטי הפונה + סטטוס)
```

- **Producer → Program**: יחס *אחד‑לרבים* — לכל מפיקה יכולות להיות מספר תוכניות.
- **Producer → Subscription**: יחס *אחד‑לאחד* — לכל מפיקה מנוי אחד (נוצר רק למפיקות, לא לרכזות).
- **Producer (רכזת) → SavedProgram → Program**: טבלת קשר *רבים‑לרבים* — רכזת שומרת תוכניות רבות, ותוכנית יכולה להישמר על‑ידי רכזות רבות. צירוף `(coordinatorId, programId)` מוגדר **ייחודי** כדי למנוע שמירה כפולה.
- **Inquiry**: מקשרת בין הפונה (רכזת רשומה או אנונימית), התוכנית והמפיקה — לכל פנייה תוכנית אחת ומפיקה אחת.

### טבלה: `Producer` (משתמש: מפיקה / רכזת / מנהלת)
מאחסנת את כל משתמשי המערכת. ההבחנה בין התפקידים נעשית באמצעות השדה `role`.

| שדה | סוג | תיאור |
|---|---|---|
| `id` | ObjectId | מזהה ייחודי (מפתח ראשי) |
| `name` | String | שם המשתמש |
| `email` | String (unique) | אימייל — ייחודי, משמש להתחברות (נשמר תמיד באותיות קטנות) |
| `phone` | String | טלפון ליצירת קשר |
| `institution` | String? | שם המוסד (רלוונטי לרכזת, אופציונלי) |
| `passwordHash` | String | סיסמה **מוצפנת** (bcrypt) — לעולם לא נשמרת כטקסט גלוי |
| `role` | String | תפקיד: `producer` / `coordinator` / `admin` (ברירת מחדל: `producer`) |
| `subscription` | Subscription? | קשר למנוי (אחד‑לאחד, למפיקות בלבד) |
| `programs` | Program[] | קשר לתוכניות (אחד‑לרבים) |
| `savedItems` | SavedProgram[] | התוכניות ששמרה (כרכזת) |
| `inquiriesSent` | Inquiry[] | פניות ששלחה (כרכזת) |
| `inquiriesReceived` | Inquiry[] | פניות שקיבלה (כמפיקה) |
| `createdAt` | DateTime | תאריך יצירה |

### טבלה: `Program` (תוכנית)
מאחסנת את התוכניות שהמפיקות מעלות.

| שדה | סוג | תיאור |
|---|---|---|
| `id` | ObjectId | מזהה ייחודי |
| `title` | String | שם התוכנית |
| `description` | String | תיאור מפורט |
| `category` | String | קטגוריה (תוכניות / הרצאות / אטרקציות / אתרי נופש / מסעדות / מדריכות טיולים) |
| `minAge` | Int | גיל מינימלי של קהל היעד |
| `maxAge` | Int | גיל מקסימלי של קהל היעד |
| `audience` | Audience (enum) | קהל יעד: `MEN` (בנים) / `WOMEN` (בנות) / `BOTH` (כולם, ברירת מחדל) |
| `duration` | String | משך התוכנית |
| `location` | String | מיקום גיאוגרפי |
| `price` | Float? | מחיר (אופציונלי) |
| `phone` | String? | טלפון ספציפי לתוכנית (אופציונלי) |
| `email` | String? | אימייל ספציפי לתוכנית (אופציונלי) |
| `tags` | String[] | תגיות |
| `images` | String[] | קישורי תמונות (Cloudinary) |
| `videos` | String[] | קישורי סרטונים (Cloudinary) |
| `status` | String | סטטוס: `pending` / `approved` / `rejected` (ברירת מחדל: `pending`) |
| `views` | Int | **מונה צפיות** — כמה פעמים נפתחו פרטי התוכנית באלפון (ברירת מחדל: 0) |
| `producerId` | ObjectId (FK) | מזהה המפיקה הבעלים |
| `savedBy` | SavedProgram[] | שמירות של רכזות על התוכנית |
| `inquiries` | Inquiry[] | פניות שהתקבלו על התוכנית |
| `createdAt` | DateTime | תאריך יצירה |
| `updatedAt` | DateTime | תאריך עדכון אחרון (מתעדכן אוטומטית) |

### טבלה: `Subscription` (מנוי)
מאחסנת את מצב המנוי של כל מפיקה.

| שדה | סוג | תיאור |
|---|---|---|
| `id` | ObjectId | מזהה ייחודי |
| `startDate` | DateTime | תאריך תחילת המנוי (ברירת מחדל: עכשיו) |
| `expiryDate` | DateTime | תאריך תפוגה (בהרשמה: שנה מהיום) |
| `status` | String | מצב: `active` / `expired` / `paused` |
| `reminderSent` | Boolean | האם נשלחה תזכורת חידוש |
| `producerId` | ObjectId (FK, unique) | מזהה המפיקה (ייחודי — מבטיח יחס אחד‑לאחד) |

### טבלה: `SavedProgram` (תוכנית שמורה של רכזת)
טבלת הקשר בין רכזת לתוכניות שהיא שמרה לאזור האישי — מועדפים + הערה אישית + סטטוס מעקב.

| שדה | סוג | תיאור |
|---|---|---|
| `id` | ObjectId | מזהה ייחודי |
| `note` | String? | הערה אישית חופשית (עד 1,000 תווים) |
| `trackStatus` | String | סטטוס מעקב: `saved` (שמורה) / `contacted` (יצרתי קשר) / `closed` (סגרתי) / `irrelevant` (לא רלוונטי) |
| `coordinatorId` | ObjectId (FK) | מזהה הרכזת (מחיקת הרכזת מוחקת גם את השמירות — Cascade) |
| `programId` | ObjectId (FK) | מזהה התוכנית (מחיקת התוכנית מוחקת גם את השמירות — Cascade) |
| `createdAt` / `updatedAt` | DateTime | תאריכי יצירה ועדכון |

> **אילוץ ייחודיות:** `@@unique([coordinatorId, programId])` — רכזת אינה יכולה לשמור את אותה תוכנית פעמיים.

### טבלה: `Inquiry` (פנייה למפיקה)
פנייה מרכזת (או גולשת אנונימית) אל מפיקה בנוגע לתוכנית מסוימת.

| שדה | סוג | תיאור |
|---|---|---|
| `id` | ObjectId | מזהה ייחודי |
| `message` | String | תוכן הפנייה |
| `status` | String | סטטוס אצל המפיקה: `new` (חדשה) / `read` (נקראה) / `closed` (טופלה) |
| `contactName` | String | שם הפונה |
| `contactPhone` | String | טלפון הפונה |
| `contactEmail` | String? | אימייל הפונה (אופציונלי) |
| `contactInstitution` | String? | מוסד הפונה (אופציונלי; לרכזת רשומה נלקח אוטומטית מהפרופיל) |
| `coordinatorId` | ObjectId? (FK) | מזהה הרכזת הפונה — `null` בפנייה אנונימית (במחיקת הרכזת השדה מתאפס — SetNull) |
| `programId` | ObjectId (FK) | התוכנית שעליה נשלחה הפנייה (Cascade) |
| `producerId` | ObjectId (FK) | המפיקה שאליה מיועדת הפנייה (Cascade) |
| `createdAt` | DateTime | תאריך שליחת הפנייה |

> **הערה:** השדות `phone` ו‑`email` קיימים גם ברמת המפיקה וגם ברמת התוכנית. הדבר מאפשר למפיקה להגדיר פרטי קשר שונים לכל תוכנית; בהיעדרם, המערכת חוזרת אוטומטית לפרטי המפיקה (ראו [לוגיקת fallback](#לוגיקת-fallback-בפרטי-קשר)).

<div style="page-break-after: always;"></div>

---

## פונקציות ולוגיקה מרכזית

פרק זה מתאר את הפעולות המרכזיות "מאחורי הקלעים" של המערכת.

### 1. אימות והרשאות (Authentication & Authorization)
ממומש בקבצים `lib/auth.ts` ו‑`lib/api-auth.ts` באמצעות **JWT** (JSON Web Token):
- **`createToken(payload)`** — יוצר טוקן חתום הכולל `producerId`, `email` ו‑**`role`** (התפקיד), בתוקף ל‑7 ימים.
- **`verifyToken(token)`** — מאמת את חתימת הטוקן ומחזיר את תוכנו.
- **`validateAuth(request)`** — שולף את הטוקן מכותרת `Authorization: Bearer ...` ומאמת אותו.
- **`requireRole(request, ...roles)`** — שכבת הרשאות לפי תפקיד: כל נתיב API מוגן מצהיר אילו תפקידים רשאים לגשת אליו (למשל: ניהול תוכניות — `producer`; תוכניות שמורות — `coordinator`; אישור תוכניות — `admin`). תפקיד לא מתאים ⇐ `403 Forbidden`.
- **ניתוב לפי תפקיד** — לאחר התחברות, כל משתמש מופנה אוטומטית לעמוד הבית שלו: מנהלת ⇐ `/admin`, רכזת ⇐ `/coordinator`, מפיקה ⇐ `/dashboard`.
- בצד הלקוח, הטוקן נשמר ב‑`localStorage` ונשלח בכל בקשה מאומתת.

**בקרת הרשאות מנהלת:** בפעולות רגישות השרת מאמת את התפקיד גם מול בסיס הנתונים, ולא נסמך רק על תוכן הטוקן. בנוסף, מנהלת אינה יכולה להירשם דרך הטופס — טופס ההרשמה מאפשר רק `producer` או `coordinator`, וחשבון המנהלת מוגדר בנפרד (סקריפט `prisma/migrate-admin-role.ts` + מנגנון המוודא בהתחברות שחשבון המנהלת מקבל את התפקיד הנכון).

### 2. הצפנת סיסמאות ונרמול אימייל
ממומש עם **bcryptjs**:
- בהרשמה: `bcrypt.hash(password, 10)` — הסיסמה מוצפנת לפני השמירה.
- בהתחברות: `bcrypt.compare(password, passwordHash)` — השוואה מאובטחת ללא פענוח. סיסמה גלויה לעולם אינה נשמרת.
- **נרמול אימייל** — האימייל מומר לאותיות קטנות (`trim` + `toLowerCase`) גם בהרשמה וגם בהתחברות, כך שמשתמשת שנרשמה עם `Name@Gmail.com` תוכל להתחבר עם `name@gmail.com`.

### 3. אימות וניקוי קלט (Validation & Sanitization)
בקובץ `lib/validation.ts` באמצעות ספריית **Zod**:
- **`loginSchema`** — אוכף אימייל תקין וסיסמה באורך 6–100 תווים.
- **`inquirySchema`** — מאמת את שדות טופס הפנייה (שם, טלפון, הודעה — חובה; אימייל ומוסד — רשות).
- **`sanitizeString`** — מנקה קלט מתוכן זדוני: מסיר תגי `<script>`, `javascript:` ומאזיני אירועים (`onclick=` וכד'), כהגנה מפני התקפות XSS. מופעל על כל שדות הפנייה לפני השמירה.

### 4. חיפוש וסינון תוכניות
בנתיב `GET /api/programs` נבנה אובייקט שאילתה דינמי:
- האלפון הציבורי מציג רק תוכניות **מאושרות**.
- **חיפוש חופשי** — מתבצע על הכותרת והתיאור (`OR`, ללא תלות באותיות גדולות/קטנות).
- **קטגוריה** — התאמה מדויקת.
- **גיל** — הרכזת מזינה גיל אחד, והמערכת מאתרת תוכניות שהטווח שלהן מכיל אותו: `minAge ≤ גיל ≤ maxAge`.
- **קהל יעד** — סינון לפי `audience`: בחירת "בנים" או "בנות" מחזירה גם תוכניות המיועדות לכולם (`BOTH`).
- **מיקום** — **חיפוש חלקי** (`contains`), כך שחיפוש "ירושלים" ימצא גם "ירושלים והסביבה".
- **מחיר** — סינון לפי מחיר מקסימלי (`lte`).
- התוצאות ממוינות מהחדש לישן וכוללות את פרטי המפיקה (ללא שדות רגישים).

### 5. מנגנון התראות במייל
בקובץ `lib/email.ts` באמצעות **Nodemailer**, ארבעה סוגי הודעות מעוצבות (HTML, RTL):
- **`sendProgramPendingEmail`** — בעת הגשת תוכנית ("ממתינה לאישור").
- **`sendProgramApprovedEmail`** — בעת אישור התוכנית.
- **`sendProgramRejectedEmail`** — בעת דחיית התוכנית.
- **`sendNewInquiryEmail`** — בעת קבלת פנייה חדשה: מייל למפיקה עם פרטי הפונה ותוכן ההודעה.

שליחת המייל עטופה ב‑`try/catch` כך ש**כשל בשליחת המייל לא מפיל את שמירת הנתונים**. בנוסף, בשליחת פנייה המייל נשלח באמצעות `after()` — **אחרי** שהתשובה חוזרת לפונה, כך שהפונה אינה ממתינה לשרת המיילים.

### 6. ניהול מחזור חיי תוכנית (Status Lifecycle)
- תוכנית חדשה / מעודכנת נשמרת תמיד בסטטוס `pending` (חוזרת לאישור גם לאחר עריכה).
- המנהלת מעבירה ל‑`approved` או `rejected`.
- האלפון הציבורי מציג רק תוכניות מאושרות, ופנייה ושמירה אפשריות רק על תוכנית מאושרת (נאכף גם בצד השרת).

### 7. מערכת הפניות (Inquiries)
בנתיב `/api/inquiries`:
- **שליחה (POST)** — פתוחה לכולם, גם ללא התחברות. אם הפונה מחוברת כרכזת, הפנייה משויכת לחשבונה ושם המוסד נלקח אוטומטית מהפרופיל; אחרת הפנייה נשמרת כאנונימית עם פרטי הקשר שמולאו בטופס.
- **צפייה וניהול (GET/PUT)** — למפיקה בלבד: שליפת הפניות שלה (עם מונה פניות חדשות) ועדכון סטטוס (חדשה / נקראה / טופלה). עדכון הסטטוס מתבצע עם סינון בעלות (`updateMany({ id, producerId })`) — עדכון ובדיקת הרשאה בשאילתה אחת.

### 8. תוכניות שמורות של רכזת (Saved Programs)
בנתיב `/api/coordinator/saved` (לרכזות בלבד):
- **שמירה/הסרה (POST)** — פעולת Toggle: לחיצה על ⭐ שומרת, לחיצה חוזרת מסירה. מקרה קצה של שתי לחיצות מהירות (Race Condition, שגיאת `P2002` על האילוץ הייחודי) מטופל בחן — התוכנית פשוט נשארת שמורה.
- **עדכון (PUT)** — הערה אישית (מוגבלת ל‑1,000 תווים) וסטטוס מעקב (מאומת מול רשימת ערכים חוקיים).
- **חישוב תקציב** — בצד הלקוח: סכימת מחירי התוכניות השמורות (ללא "לא רלוונטי") + תקציב "סגור" של תוכניות שסומנו "סגרתי".

### 9. סטטיסטיקות למפיקה
- **מונה צפיות** — בכל פתיחת חלון פרטי תוכנית באלפון נקרא `POST /api/programs/[id]/view` המגדיל את `views`.
- **`GET /api/dashboard/stats`** — מרכז לכל תוכנית של המפיקה את מספר הצפיות, הפניות והשמירות (באמצעות שאילתות `groupBy`), וכן סיכומים כוללים ומונה פניות חדשות המוצג בדשבורד.

### 10. בעלות ואבטחת פעולות (Ownership)
בעת עריכה/מחיקת תוכנית על‑ידי מפיקה, השרת מאמת ש‑`producerId` של התוכנית תואם למשתמש המחובר — כך מפיקה אינה יכולה לגעת בתוכניות של אחרת. אותו עיקרון חל על פניות (רק המפיקה שקיבלה אותן) ועל תוכניות שמורות (רק הרכזת ששמרה אותן).

### 11. הגנות ברמת ה‑Middleware
בקובץ `middleware.ts`, החלות על כל נתיבי `/api`, `/dashboard`, `/admin`, `/coordinator`:
- **Rate Limiting** — עד 100 בקשות לדקה לכל כתובת IP (מחזיר `429`). המכסה נבחרה כך שגלישה רגילה — וגם מוסד שלם מאחורי כתובת IP משותפת — לא תיחסם.
- **הגנת CSRF** — חסימת בקשות `POST` שמקורן (`origin`) אינו תואם ל‑`host` (מחזיר `403`).
- **כותרות אבטחה** — `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, `X-XSS-Protection`.

### לוגיקת fallback בפרטי קשר
בחלון הצגת התוכנית (Modal), פרטי הקשר נקבעים לפי סדר עדיפות:
1. אם הוגדר טלפון/אימייל **ספציפי לתוכנית** — הוא מוצג.
2. אחרת — מוצגים פרטי **המפיקה**.
3. אם אין כלל אימייל — מוצגת הודעה "לא זמין".

<div style="page-break-after: always;"></div>

---

## מבנה המערכת (Client / Server / Database)

המערכת בנויה בארכיטקטורת **Full-Stack** מבוססת **Next.js**, המאחדת לקוח ושרת בפרויקט אחד.

```
┌──────────────────────────────────────────────────────────────┐
│                         CLIENT (דפדפן)                          │
│   React 19 + Next.js App Router + TypeScript + Tailwind CSS    │
│   דפים: בית · התחברות · הרשמה · אלפון · אזור רכזת ·             │
│         דשבורד מפיקה · הפניות שלי · ניהול                       │
└───────────────────────────────┬──────────────────────────────┘
                                 │  HTTP (JSON) + JWT
                                 ▼
┌──────────────────────────────────────────────────────────────┐
│                    MIDDLEWARE (שכבת הגנה)                       │
│        Rate Limiting · CSRF · Security Headers                 │
└───────────────────────────────┬──────────────────────────────┘
                                 ▼
┌──────────────────────────────────────────────────────────────┐
│                    SERVER (Next.js API Routes)                 │
│   /api/register · /api/auth · /api/programs ·                  │
│   /api/inquiries · /api/coordinator/* ·                        │
│   /api/dashboard · /api/dashboard/stats · /api/admin/programs  │
│   לוגיקה: JWT + requireRole · bcrypt · Zod · Nodemailer        │
└──────────────┬───────────────────────────┬───────────────────┘
               │ Prisma ORM                 │  (העלאת מדיה)
               ▼                            ▼
┌──────────────────────────┐   ┌──────────────────────────────┐
│      MongoDB (DB)         │   │         Cloudinary           │
│  Producer · Program ·     │   │   אחסון תמונות וסרטונים       │
│  Subscription ·           │   │                              │
│  SavedProgram · Inquiry   │   │                              │
└──────────────────────────┘   └──────────────────────────────┘
```

### Frontend (צד לקוח)
| טכנולוגיה | תפקיד |
|---|---|
| **Next.js 16 (App Router)** | מסגרת העבודה, ניתוב, רינדור |
| **React 19** | בניית ממשק המשתמש |
| **TypeScript 5** | טיפוסיות סטטית ובטיחות קוד |
| **Tailwind CSS 4** | עיצוב רספונסיבי ותמיכה ב‑RTL |
| **lucide-react** | אייקונים |
| **next-cloudinary** | רכיב העלאת תמונות וסרטונים |

### Backend (צד שרת)
| טכנולוגיה | תפקיד |
|---|---|
| **Next.js API Routes** | נקודות קצה RESTful |
| **Prisma ORM 6** | גישה לבסיס הנתונים וניהול הסכמה |
| **jsonwebtoken (JWT)** | אימות משתמשים |
| **bcryptjs** | הצפנת סיסמאות |
| **Zod** | אימות נתונים |
| **Nodemailer** | שליחת מיילים |

### Database & Storage
| טכנולוגיה | תפקיד |
|---|---|
| **MongoDB** | בסיס נתונים מסמכי (NoSQL) בענן |
| **Cloudinary** | אחסון וסטרימינג של מדיה |

### נקודות הקצה (API Endpoints)
| נתיב | מתודות | תיאור | הרשאה |
|---|---|---|---|
| `/api/register` | `POST` | הרשמת מפיקה (+ יצירת מנוי) או רכזת | ציבורי |
| `/api/auth` | `POST` | התחברות והנפקת טוקן | ציבורי |
| `/api/programs` | `GET` | אלפון התוכניות המאושרות (חיפוש/סינון) | ציבורי |
| `/api/programs/[id]/view` | `POST` | הגדלת מונה הצפיות של תוכנית | ציבורי |
| `/api/inquiries` | `POST` | שליחת פנייה למפיקה על תוכנית | ציבורי (גם אנונימי) |
| `/api/inquiries` | `GET / PUT` | צפייה בפניות ועדכון סטטוס | מפיקה מחוברת |
| `/api/dashboard` | `GET / POST / PUT / DELETE` | ניהול תוכניות המפיקה | מפיקה מחוברת |
| `/api/dashboard/stats` | `GET` | סטטיסטיקות: צפיות / פניות / שמירות | מפיקה מחוברת |
| `/api/coordinator/saved` | `GET / POST / PUT / DELETE` | תוכניות שמורות: שמירה, הערות, מעקב | רכזת מחוברת |
| `/api/coordinator/profile` | `GET` | פרופיל הרכזת (למילוי אוטומטי בטפסים) | רכזת מחוברת |
| `/api/admin/programs` | `GET / PUT / DELETE` | ניהול ואישור כל התוכניות | מנהלת בלבד |

<div style="page-break-after: always;"></div>

---

## מדריך משתמש לפי סוגי משתמשים

במערכת ארבעה סוגי משתמשים. להלן פירוט מה כל אחד **רואה** ומה הוא **יכול לעשות**.

### 🔎 משתמש 1: גולשת אורחת (ללא הרשמה)

**מה היא רואה:**
- עמוד הבית עם הסבר קצר וכפתורי פעולה.
- עמוד "אלפון התוכניות" עם רשימת כל התוכניות **המאושרות**.
- חלון פרטים מלא לכל תוכנית.

**מה היא יכולה לעשות:**
1. לפתוח את האלפון ללא הרשמה.
2. לחפש תוכנית בחיפוש חופשי.
3. לסנן לפי קטגוריה, גיל, קהל יעד, מיקום ומחיר מקסימלי.
4. ללחוץ על תוכנית ולצפות בכל פרטיה (מדיה, תיאור, מחיר).
5. לקבל את פרטי הקשר של המפיקה וליצור קשר ישיר.
6. **לשלוח פנייה למפיקה דרך האתר** — מילוי טופס קצר (שם, טלפון, אימייל, מוסד והודעה) מתוך חלון התוכנית.

---

### 👩‍🏫 משתמש 2: רכזת רשומה (Coordinator)

**מה היא רואה:**
- כל מה שרואה גולשת אורחת, ובנוסף כפתור ⭐ על כל תוכנית באלפון.
- **אזור אישי** (`/coordinator`) עם התוכניות ששמרה, כרטיסי סיכום ותקציב.

**מה היא יכולה לעשות:**
1. **להירשם כרכזת** — הרשמה ייעודית (ללא מנוי וללא תשלום) הכוללת גם שם מוסד.
2. **לשמור תוכניות** מהאלפון בלחיצת ⭐, ולהסיר בלחיצה חוזרת.
3. לכתוב **הערה אישית** על כל תוכנית שמורה ("דיברתי איתה, פנויה רק בחנוכה").
4. לסמן **סטטוס מעקב** לכל תוכנית: שמורה / יצרתי קשר / סגרתי / לא רלוונטי.
5. לראות **חישוב תקציב אוטומטי**: סך עלויות התוכניות הרלוונטיות + תקציב סגור (תוכניות שסומנו "סגרתי").
6. **לשלוח פנייה למפיקה** — גם מהאלפון וגם מהאזור האישי; פרטיה מולאו מראש מהפרופיל, והפנייה משויכת לחשבונה.

---

### 👩‍💼 משתמש 3: מפיקה (Producer)

**מה היא רואה:**
- דשבורד אישי עם ברכת שלום ו**מצב המנוי** (פעיל / מתקרב לפקיעה / פג תוקף).
- **סטטיסטיקות**: צפיות, פניות ושמירות — לכל תוכנית ובסך הכול.
- רשימת **התוכניות שלה בלבד**, כל אחת עם הסטטוס שלה.
- עמוד **"הפניות שלי"** עם כל הפניות שהתקבלו ומונה פניות חדשות.
- טופס הוספה/עריכה של תוכנית.

**מה היא יכולה לעשות:**
1. **להירשם** ולקבל מנוי שנתי אוטומטי.
2. **להתחבר / להתנתק**.
3. **להוסיף תוכנית חדשה** — כולל טווח גילאים, קהל יעד, תמונת פרסום וסרטון טריילר.
4. **לערוך** תוכנית קיימת (חוזרת לאישור מחדש).
5. **למחוק** תוכנית.
6. לעקוב אחר **סטטוס** כל תוכנית (ממתין / מאושר / נדחה).
7. לצפות ב**פניות** שהתקבלו, לסנן לפי סטטוס ולסמן: חדשה / נקראה / טופלה.
8. לעקוב אחרי **ביצועי התוכניות**: כמה צפו, כמה פנו וכמה רכזות שמרו.
9. לקבל **התראות במייל**: קליטת תוכנית, אישור, דחייה ופנייה חדשה.

---

### 🛡️ משתמש 4: מנהלת (Admin)

**מה היא רואה:**
- פאנל ניהול ייעודי עם **סטטיסטיקות** (סך התוכניות, ממתינות, מאושרות, נדחות).
- רשימת **כל** התוכניות במערכת, כולל **פרטי המפיקה** (שם, טלפון, אימייל).
- מסנן סטטוס.

**מה היא יכולה לעשות:**
1. להתחבר ולהיות מופנית אוטומטית לפאנל הניהול.
2. לסנן תוכניות לפי סטטוס.
3. **לאשר** תוכנית — היא מתפרסמת באלפון, ונשלח מייל למפיקה.
4. **לדחות** תוכנית — נשלח מייל דחייה.
5. **למחוק** תוכנית לצמיתות.

> **הגדרת חשבון המנהלת:** מנהלת אינה נרשמת דרך טופס ההרשמה. החשבון מקודם לתפקיד `admin` באמצעות סקריפט ייעודי (`npm run migrate:role`), ובנוסף קיים מנגנון בטיחות המוודא בעת ההתחברות שחשבון המנהלת המוגדר מראש מקבל את התפקיד הנכון.

<div style="page-break-after: always;"></div>

---

## תיאור מסכים

> 💡 **הערה להגשה:** בכל מסך מומלץ לשבץ **צילום מסך** מהמערכת הפועלת במקום המסומן.

### מסך 1 — עמוד הבית (`/`)
מסך הנחיתה. כולל סרגל ניווט עליון, כותרת ראשית עם תיאור הפלטפורמה, ושני כפתורי פעולה מרכזיים: **"חיפוש תוכניות"** (לרכזות) ו**"הצטרפי כמפיקה"** (להרשמה). בתחתית — פוטר עם זכויות יוצרים.

`⟪ צילום מסך: עמוד הבית ⟫`

---

### מסך 2 — הרשמה (`/register`)
טופס הרשמה משותף עם **בחירת תפקיד**: מפיקה או רכזת (הכפתור בעמוד הבית קובע את ברירת המחדל). השדות: שם מלא, אימייל, טלפון, סיסמה ואימות סיסמה; לרכזת נוסף שדה **שם מוסד**. הטופס בודק שהסיסמאות תואמות ושאורך הסיסמה תקין לפני השליחה. בהצלחה המשתמשת מחוברת אוטומטית ומועברת לאזור שלה: מפיקה ⇐ דשבורד, רכזת ⇐ אזור אישי.

`⟪ צילום מסך: מסך הרשמה — בחירת תפקיד ⟫`

---

### מסך 3 — התחברות (`/login`)
טופס התחברות (אימייל + סיסמה). בעת הצלחה, ניתוב חכם לפי תפקיד: **מנהלת** מופנית ל‑`/admin`, **רכזת** ל‑`/coordinator`, ו**מפיקה** ל‑`/dashboard`. שגיאות מוצגות בהודעה ברורה.

`⟪ צילום מסך: מסך התחברות ⟫`

---

### מסך 4 — אלפון התוכניות (`/programs`)
המסך המרכזי לרכזות. בראשו **לוח סינון** (חיפוש חופשי, קטגוריה, גיל, קהל יעד, מיקום, מחיר מקסימלי), ומתחתיו רשימת התוכניות המאושרות ככרטיסים. רכזת מחוברת רואה על כל כרטיס כפתור **⭐ לשמירה** מהירה. מצב טעינה מוצג בזמן שליפה, והודעה ידידותית כשאין תוצאות. לחיצה על תוכנית פותחת את חלון הפרטים.

`⟪ צילום מסך: אלפון התוכניות + פילטרים ⟫`

---

### מסך 5 — חלון פרטי תוכנית (Modal)
חלון קופץ המציג את כל פרטי התוכנית: תמונות, סרטון, תיאור, תג קטגוריה, כרטיסיות גילאים / קהל יעד / מיקום / משך / מחיר, ובתחתית — **בלוק יצירת קשר** עם טלפון ואימייל (לפי לוגיקת ה‑fallback) וכפתור **"שליחת פנייה"**. פתיחת החלון מגדילה את מונה הצפיות של התוכנית.

`⟪ צילום מסך: חלון פרטי תוכנית ⟫`

---

### מסך 6 — טופס פנייה למפיקה (Modal)
נפתח מחלון פרטי התוכנית (או מכרטיס תוכנית שמורה באזור הרכזת). כולל: שם מלא, טלפון, אימייל, מוסד והודעה חופשית. לרכזת מחוברת השדות מתמלאים אוטומטית מהפרופיל. לאחר השליחה מוצג אישור, והמפיקה מקבלת מייל התראה מעוצב.

`⟪ צילום מסך: טופס פנייה ⟫`

---

### מסך 7 — אזור אישי לרכזת (`/coordinator`)
מסך הבית של הרכזת: ברכת שלום, **כרטיסי סיכום** (מספר תוכניות שמורות, תקציב משוער ללא "לא רלוונטי", ותקציב סגור), ומתחתיהם רשימת **התוכניות השמורות** ככרטיסים. בכל כרטיס: פרטי התוכנית ופרטי המפיקה, שדה **הערה אישית**, בורר **סטטוס מעקב** (שמורה / יצרתי קשר / סגרתי / לא רלוונטי), כפתור פנייה למפיקה וכפתור הסרה. כשאין תוכניות שמורות — הסבר ידידותי המפנה לאלפון.

`⟪ צילום מסך: אזור אישי לרכזת ⟫`

---

### מסך 8 — דשבורד המפיקה (`/dashboard`)
מסך הבית של המפיקה: כרטיס ברכה אישי, **רכיב מצב מנוי** צבעוני (ירוק/כתום/אדום לפי הימים שנותרו), **סקירת סטטיסטיקות** (סך צפיות, פניות ושמירות + פירוט לכל תוכנית), קישור לעמוד **"הפניות שלי"** עם מונה פניות חדשות, וכפתור "הוסף תוכנית". התוכניות מוצגות ככרטיסים עם אפשרויות עריכה ומחיקה. כשאין תוכניות — מוצג מסך עידוד ליצירת התוכנית הראשונה.

`⟪ צילום מסך: דשבורד המפיקה ⟫`

---

### מסך 9 — הוספת / עריכת תוכנית (`/dashboard/new`, `/dashboard/edit/[id]`)
טופס מלא לתוכנית: שם, תיאור, העלאת **תמונת פרסום** ו**סרטון טריילר** (Cloudinary), קטגוריה, **טווח גילאים** (מגיל–עד גיל), **קהל יעד** (בנים / בנות / כולם), משך, מיקום, מחיר ופרטי קשר. בטופס תוכנית חדשה, פרטי הקשר מתמלאים אוטומטית מפרטי המפיקה. מסך העריכה זהה במבנה וטעון בנתוני התוכנית הקיימת.

`⟪ צילום מסך: טופס הוספת תוכנית ⟫`

---

### מסך 10 — הפניות שלי (`/dashboard/inquiries`)
עמוד ניהול הפניות של המפיקה: רשימת כל הפניות שהתקבלו (חדשות תחילה), עם שם התוכנית, פרטי הפונה (שם, טלפון, אימייל, מוסד) ותוכן ההודעה. ניתן לסנן לפי סטטוס ולסמן כל פנייה: **חדשה / נקראה / טופלה**. פניות חדשות מודגשות ומופיעות במונה בדשבורד.

`⟪ צילום מסך: עמוד הפניות שלי ⟫`

---

### מסך 11 — פאנל ניהול (`/admin`)
מסך המנהלת: כרטיסי **סטטיסטיקה** (הכל / ממתין / מאושר / נדחה) המשמשים גם כמסנן, ורשימת תוכניות מפורטת הכוללת פרטי תוכנית, **פרטי מפיקה**, תג סטטוס צבעוני, וכפתורי פעולה: **אשר / דחה / מחק**.

`⟪ צילום מסך: פאנל הניהול ⟫`

<div style="page-break-after: always;"></div>

---

## סיכום

### מה למדתי
פרויקט זה היווה עבורי תהליך למידה מעמיק שחיבר בין כל חלקי הפיתוח לכדי מערכת אחת שלמה. בין היתר רכשתי ידע מעשי ב:
- בנייה של אפליקציית **Full-Stack** מודרנית בארכיטקטורת **Next.js** (לקוח ושרת יחד).
- עבודה עם בסיס נתונים **MongoDB** באמצעות **Prisma ORM**, כולל תכנון סכמה, קשרים (אחד‑לאחד, אחד‑לרבים, רבים‑לרבים דרך טבלת קשר), אילוצי ייחודיות ושאילתות צבירה (`groupBy`).
- מימוש **אבטחת מידע** אמיתית: הצפנת סיסמאות, JWT, **בקרת הרשאות מבוססת תפקידים** (מפיקה / רכזת / מנהלת), Rate Limiting והגנת CSRF.
- כתיבת קוד **טיפוסי ובטוח** עם TypeScript, ואימות נתונים עם Zod.
- שילוב **שירותי צד שלישי** (Cloudinary למדיה, Nodemailer למיילים).
- עיצוב **חוויית משתמש** רספונסיבית ונגישה בעברית (RTL).
- ביצוע **מיגרציות נתונים** על מערכת קיימת (מעבר מ‑`isAdmin` ל‑`role`, המרת גיל מטרה טקסטואלי לטווח גילאים מספרי) ללא אובדן נתונים.
- כתיבת **בדיקות אוטומטיות** (Jest + React Testing Library) לרכיבים וללוגיקה.

### קשיים ופתרונות
> *הקשיים הבאים מבוססים על השינויים והפתרונות הקיימים בקוד עצמו.*

| הקושי | הפתרון שיושם |
|---|---|
| חיפוש לפי מיקום בהתאמה מדויקת החמיץ תוצאות (למשל "ירושלים" לעומת "ירושלים והסביבה"). | מעבר ל**חיפוש חלקי** (`contains`, ללא תלות באותיות) שמרחיב את התוצאות הרלוונטיות. |
| גיל המטרה נשמר כטקסט חופשי ("בנות 6-12") — לא ניתן היה לסנן לפי גיל אמיתי. | פיצול לשדות מספריים **`minAge` / `maxAge`** + שדה **קהל יעד** (enum), וסקריפט מיגרציה שהמיר את הנתונים הקיימים. הסינון הפך מדויק: "בת 8" מוצאת כל תוכנית שהטווח שלה כולל 8. |
| משתמשת שנרשמה עם אותיות גדולות באימייל (`Name@Gmail.com`) לא הצליחה להתחבר. | **נרמול אימייל** (`trim` + `toLowerCase`) אחיד בהרשמה ובהתחברות. |
| לאחר המעבר מ‑`isAdmin` ל‑`role`, חשבון המנהלת בפרודקשן נשאר עם תפקיד שגוי ואיבד גישה לפאנל. | סקריפט מיגרציה חד‑פעמי (`npm run migrate:role`) + מנגנון בטיחות בהתחברות המוודא שחשבון המנהלת המוגדר מקבל `role: admin`. |
| שתי לחיצות מהירות על ⭐ יצרו מרוץ (Race Condition) וניסיון שמירה כפולה של אותה תוכנית. | אילוץ ייחודיות `(coordinatorId, programId)` בבסיס הנתונים + טיפול בשגיאת `P2002` כך שהמערכת מזהה שהתוכנית כבר שמורה. |
| נקודות קצה החזירו את אובייקט המפיקה המלא — כולל `passwordHash`. | צמצום כל השאילתות ל‑`select` מפורש של השדות הנחוצים בלבד — סיסמה מוצפנת אינה עוזבת את השרת. |
| שליחת מייל ההתראה עיכבה את הפונה עד לתשובת שרת ה‑SMTP, וכשל בשליחה היה עלול להפיל את השמירה. | עטיפת שליחת המייל ב‑`try/catch`, ובפניות — שליחה באמצעות `after()` **אחרי** החזרת התשובה. הנתונים נשמרים תמיד והפונה אינה ממתינה. |
| צורך בפרטי קשר שונים לכל תוכנית, מבלי לחייב מילוי חוזר. | הוספת שדות טלפון/אימייל ברמת התוכנית + **לוגיקת fallback** לפרטי המפיקה, ומילוי אוטומטי בטופס. |
| מניעת גישה לא מורשית לפעולות ניהול ולתוכניות של מפיקות אחרות. | שכבת `requireRole` בכל נתיב מוגן, ואימות **בעלות** (`producerId` / `coordinatorId`) בכל פעולת עריכה/מחיקה. |
| חשיפה להתקפות אוטומטיות ולקלט זדוני. | שכבת Middleware (Rate Limiting + CSRF + כותרות אבטחה) ופונקציית `sanitizeString` נגד XSS. |

`⟪ ניתן להוסיף כאן קשיים אישיים נוספים שחווית במהלך הפיתוח ⟫`

### סיכום ומבט קדימה
המערכת **מדריך תוכניות** מהווה פתרון מלא ומקצה‑לקצה לבעיה אמיתית בשוק האירועים, ומשרתת בהצלחה ארבעה סוגי משתמשים בעלי צרכים שונים. הפרויקט עומד ביעדים שהוגדרו בהצעה המקורית — ואף מעבר להם: במהלך הפיתוח נוספו אזור אישי לרכזת, מערכת פניות מלאה וסטטיסטיקות למפיקה, שהפכו את המערכת מאלפון סטטי לכלי עבודה יומיומי לשני צידי השוק — תוך הקפדה על אבטחה, חוויית משתמש ואיכות קוד.

**כיווני פיתוח עתידיים:**
- מנוע **חיפוש מבוסס AI** להמלצות חכמות.
- מערכת **דירוגים וביקורות** לתוכניות.
- שילוב **מערכת תשלומים** אמיתית לחידוש מנויים.
- **התראות בזמן אמת** (בתוך האתר) על פניות חדשות, בנוסף למייל.
- פיתוח **אפליקציית מובייל**.

<div align="center">

---

*© 2026 מדריך תוכניות — ספר פרויקט*

</div>

</div>
