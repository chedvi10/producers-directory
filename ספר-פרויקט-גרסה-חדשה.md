<div dir="rtl">

<div align="center">

# מדריך תוכניות
### ספר פרויקט - גרסה חדשה ומקצועית

---

| | |
|---|---|
| שם הפרויקט | מדריך תוכניות |
| שם המגישה | ⟪השלמה⟫ |
| שם המנחה | ⟪השלמה⟫ |
| מוסד לימודי | ⟪השלמה⟫ |
| מסלול | ⟪השלמה⟫ |
| שנת הגשה | 2026 |

</div>

<div style="page-break-after: always;"></div>

## תודות
תודה למנחה הפרויקט, לצוות ההוראה, למשפחה ולחברות, ולמשתמשות מהשטח שסייעו לדייק את הדרישות בפועל: מפיקות, רכזות ומנהלת מערכת.

<div style="page-break-after: always;"></div>

## הקדמה / תקציר
מדריך תוכניות היא מערכת Web לניהול, איתור והפצה של תוכניות לאירועים. המערכת מחברת בין מפיקות שמפרסמות תוכניות לבין רכזות שמחפשות תוכן לאירועים. בנוסף קיימת שכבת ניהול מנהלת המאשרת או דוחה תוכניות לפני פרסום.

המערכת כוללת הרשאות לפי תפקיד, אלפון ציבורי עם חיפוש וסינון, אזור אישי לרכזת, דשבורד למפיקה, ניהול פניות, סטטיסטיקות, שליחת מיילים, ואבטחת מידע מקצה לקצה.

נקודת חוזקה מרכזית במימוש הנוכחי: עדכון תוכנית קיימת לא מחליף מיידית את מה שמוצג לציבור. בזמן שהתוכנית מעודכנת וממתינה לאישור, הציבור רואה את הגרסה המאושרת הקודמת. רק לאחר אישור מנהלת הגרסה החדשה מתפרסמת.

<div style="page-break-after: always;"></div>

## הצעת פרויקט (כפי שהוגשה)
### הבעיה
המידע על תוכניות לאירועים מפוזר, קשה לסנן אותו, וקיים קושי להגיע במהירות לתוכנית מתאימה לפי גיל, קהל יעד, מיקום ותקציב.

### המטרה
לבנות פלטפורמה אחת שמרכזת תוכניות, מאפשרת חיפוש מתקדם, ומנהלת תהליך עבודה מלא בין מפיקה לרכזת, כולל בקרת איכות של מנהלת.

### היקף
1. הרשמה והתחברות לפי תפקיד.
2. ניהול תוכניות למפיקה.
3. אלפון תוכניות ציבורי עם פילטרים.
4. אזור אישי לרכזת לשמירה ומעקב.
5. פאנל מנהלת לאישור/דחייה.
6. מערכת פניות והתראות במייל.

<div style="page-break-after: always;"></div>

## תוכן עניינים
1. מבוא
2. פירוט הפרויקט והזרימה העסקית
3. חלק טכני: מבנה נתונים
4. חלק טכני: פונקציות ולוגיקה מרכזית
5. חלק טכני: מבנה המערכת (Client / Server / Database)
6. מדריך משתמש לפי סוגי משתמשים
7. תיאור מסכים
8. סיכום

<div style="page-break-after: always;"></div>

## מבוא
### רעיון המערכת
המערכת יוצרת פלטפורמה דיגיטלית שמחברת בין מפיקות לבין רכזות. במקום חיפוש מבוזר דרך המלצות אישיות, קבוצות ופרסומים נקודתיים, כל תוכנית מוזנת למערכת במבנה קבוע, נשמרת בבסיס הנתונים, עוברת בקרת איכות, ורק לאחר מכן מוצגת לחיפוש ציבורי.

### הבעיה שהמערכת פותרת
1. מידע לא אחיד על תוכניות ואטרקציות.
2. קושי לאתר תוכנית לפי גיל, קהל יעד, אזור או תקציב.
3. חוסר בקרה על אמינות התוכן המתפרסם.
4. היעדר אזור עבודה מסודר לרכזת ולהפקה.

### הפתרון
1. אלפון תוכניות ציבורי עם מנגנון סינון מתקדם.
2. דשבורד מפיקה לניהול תוכן, פניות וסטטיסטיקות.
3. אזור אישי לרכזת לשמירה, מעקב ותקציב.
4. שכבת ניהול המאשרת או דוחה תוכניות לפני פרסום.
5. מנגנון עדכון זהיר שבו שינוי בתוכנית מאושרת אינו מחליף מיידית את הגרסה החיה לציבור.

### הערה להגשה
בחלק הטכני מומלץ מאוד לשלב שני צילומים:
1. צילום של מבנה האוספים מתוך MongoDB Compass או כלי DB דומה.
2. צילום של תרשים ERD או תרשים קשרים שנבנה לפי הטבלאות במסמך זה.

<div style="page-break-after: always;"></div>

## פירוט על הפרויקט - איך המערכת עובדת
### תהליך העבודה של מפיקה
1. המפיקה נרשמת למערכת ומזוהה כמשתמשת בתפקיד `producer`.
2. במהלך ההרשמה נוצר עבורה גם מנוי פעיל לשנה בטבלת `Subscription`.
3. לאחר ההתחברות המפיקה נכנסת לדשבורד האישי ורואה את התוכניות שלה בלבד.
4. כאשר היא יוצרת תוכנית חדשה, הנתונים נשמרים בטבלת `Program` עם סטטוס `pending`.
5. כל עוד התוכנית בסטטוס `pending`, היא אינה זמינה לצפייה ציבורית אלא ממתינה לבדיקת מנהלת.
6. כאשר המפיקה עורכת תוכנית שכבר אושרה בעבר, המערכת שומרת עותק של הגרסה המאושרת האחרונה בשדה `previousApprovedData`.
7. לאחר מכן נשמרים השינויים החדשים על אותה תוכנית והסטטוס מוחזר ל-`pending`.
8. בזמן ההמתנה לאישור מחודש, הציבור ממשיך לראות את הגרסה הישנה והמאושרת, ולא את השינויים החדשים.
9. לאחר החלטת המנהלת, הגרסה המעודכנת תאושר או שהתוכנית תידחה.

### תהליך העבודה של מנהלת
1. המנהלת נכנסת לפאנל הניהול ורואה את כלל התוכניות במערכת.
2. כל תוכנית מוצגת יחד עם פרטי המפיקה והסטטוס הנוכחי שלה.
3. אם המנהלת מאשרת את התוכנית, הסטטוס משתנה ל-`approved` והשדה `previousApprovedData` מתאפס.
4. אם המנהלת דוחה את התוכנית, הסטטוס משתנה ל-`rejected` וגם שדה הגרסה הקודמת מתאפס.
5. לאחר אישור או דחייה נשלח מייל למפיקה.

### תהליך העבודה של רכזת
1. רכזת יכולה לצפות באלפון התוכניות, גם ללא התחברות.
2. אם היא נרשמת, נוצר עבורה חשבון בתפקיד `coordinator`.
3. רכזת רשומה יכולה לשמור תוכניות, לנהל הערות אישיות, לעדכן סטטוס מעקב ולחשב תקציב.
4. מכל תוכנית ניתן לפתוח חלון פרטים, לצפות במידע המלא ולשלוח פנייה ישירה למפיקה.

### תהליך הפניות
1. כל פנייה נוצרת דרך טופס בממשק ונשלחת לנתיב השרת `/api/inquiries`.
2. הנתונים עוברים אימות וסניטיזציה לפני שמירה.
3. הפנייה נשמרת בטבלת `Inquiry` יחד עם שיוך לתוכנית ולמפיקה.
4. לאחר השמירה, המערכת מנסה לשלוח מייל התראה למפיקה.
5. גם אם שליחת המייל נכשלת, הפנייה עצמה נשארת שמורה במסד הנתונים.

<div style="page-break-after: always;"></div>

## חלק טכני: מבנה נתונים
### מבוא טכני
בסיס הנתונים של המערכת הוא MongoDB, והגישה אליו מתבצעת דרך Prisma ORM. למרות שמדובר בבסיס נתונים מסמכי, במישור התכנוני ניתן לתאר את היישויות כמו טבלאות לוגיות עם קשרים ברורים ביניהן.

### טבלאות / אוספים במערכת
1. `Producer`
2. `Program`
3. `Subscription`
4. `SavedProgram`
5. `Inquiry`
6. `Audience` בתור Enum לקהל יעד

### תרשים קשרים לוגי
```
Producer (producer) 1 ---- N Program
Producer (producer) 1 ---- 1 Subscription
Producer (coordinator) 1 ---- N SavedProgram N ---- 1 Program
Producer (coordinator) 1 ---- N Inquiry
Producer (producer)    1 ---- N Inquiry
Program                1 ---- N Inquiry
```

### מה נשמר בכל טבלה
#### טבלה 1: Producer
טבלה זו מייצגת את כל המשתמשות במערכת: מפיקה, רכזת ומנהלת. ההבדל בין הסוגים אינו נעשה באמצעות טבלה נפרדת אלא דרך שדה `role`.

| שדה | סוג | מה נשמר בפועל | הערה |
|---|---|---|---|
| `id` | ObjectId | מזהה ייחודי למשתמשת | מפתח ראשי |
| `name` | String | שם מלא | חובה |
| `email` | String | כתובת מייל להתחברות | ייחודי |
| `phone` | String | טלפון | חובה |
| `institution` | String? | שם מוסד | רלוונטי בעיקר לרכזת |
| `passwordHash` | String | סיסמה מוצפנת | לא נשמרת סיסמה גלויה |
| `role` | String | `producer` / `coordinator` / `admin` | בקרת הרשאות |
| `createdAt` | DateTime | תאריך יצירה | נוצר אוטומטית |

קשרים עיקריים:
1. למפיקה יש תוכניות רבות.
2. למפיקה יכול להיות מנוי אחד.
3. לרכזת יש תוכניות שמורות רבות.
4. לרכזת ולמפיקה יש פניות קשורות, אבל מכיוונים שונים.

#### טבלה 2: Program
זו הטבלה המרכזית במערכת. כל רשומה מייצגת תוכנית אחת שמפיקה יכולה לפרסם.

| שדה | סוג | מה נשמר בפועל | הערה |
|---|---|---|---|
| `id` | ObjectId | מזהה תוכנית | מפתח ראשי |
| `title` | String | שם התוכנית | חובה |
| `description` | String | תיאור מלא | חובה |
| `category` | String | סוג התוכנית | לדוגמה הרצאה / אטרקציה |
| `minAge` | Int | גיל מינימלי | משמש לחיפוש |
| `maxAge` | Int | גיל מקסימלי | משמש לחיפוש |
| `audience` | Enum | `MEN` / `WOMEN` / `BOTH` | קהל יעד |
| `duration` | String | משך הפעילות | טקסט חופשי |
| `location` | String | מיקום | משמש לחיפוש חלקי |
| `price` | Float? | מחיר | אופציונלי |
| `phone` | String? | טלפון ספציפי לתוכנית | אם שונה משל המפיקה |
| `email` | String? | מייל ספציפי לתוכנית | אם שונה משל המפיקה |
| `tags` | String[] | תגיות | לסיווג עתידי |
| `images` | String[] | קישורי תמונות | נשמרים כ-URL |
| `videos` | String[] | קישורי סרטונים | נשמרים כ-URL |
| `status` | String | `pending` / `approved` / `rejected` | לוגיקת פרסום |
| `previousApprovedData` | Json? | עותק גרסה מאושרת קודמת | קריטי לעדכונים |
| `views` | Int | מונה צפיות | לסטטיסטיקות |
| `producerId` | ObjectId | המפיקה הבעלים | מפתח זר לוגי |
| `createdAt` | DateTime | תאריך יצירה | אוטומטי |
| `updatedAt` | DateTime | תאריך עדכון אחרון | אוטומטי |

משמעות טכנית של `previousApprovedData`:
1. כאשר תוכנית מאושרת נערכת, לפני שמירת הנתונים החדשים נשמר snapshot של הגרסה המאושרת האחרונה.
2. כך ניתן להשאיר לציבור גרסה יציבה ותקינה בזמן שהגרסה החדשה עדיין ממתינה לאישור.
3. זהו פתרון עסקי חשוב שמונע מצב שבו תוכן לא מאושר מחליף בטעות תוכן חי.

#### טבלה 3: Subscription
טבלה זו מייצגת את מצב המנוי של מפיקה.

| שדה | סוג | מה נשמר בפועל | הערה |
|---|---|---|---|
| `id` | ObjectId | מזהה מנוי | מפתח ראשי |
| `startDate` | DateTime | תחילת המנוי | אוטומטי |
| `expiryDate` | DateTime | תוקף המנוי | מחושב בעת יצירה |
| `status` | String | `active` / `expired` / `paused` | מצב נוכחי |
| `reminderSent` | Boolean | האם נשלחה תזכורת | שימוש עתידי/ניהולי |
| `producerId` | ObjectId | שיוך למפיקה | ייחודי |

#### טבלה 4: SavedProgram
טבלת קשר בין רכזת לבין תוכנית. מעבר לשיוך עצמו נשמר כאן גם מידע עסקי נוסף.

| שדה | סוג | מה נשמר בפועל | הערה |
|---|---|---|---|
| `id` | ObjectId | מזהה רשומת שמירה | מפתח ראשי |
| `note` | String? | הערה אישית של רכזת | אופציונלי |
| `trackStatus` | String | `saved` / `contacted` / `closed` / `irrelevant` | מצב טיפול |
| `coordinatorId` | ObjectId | מזהה הרכזת | קשר ל-Producer |
| `programId` | ObjectId | מזהה התוכנית | קשר ל-Program |
| `createdAt` | DateTime | מועד שמירה | אוטומטי |
| `updatedAt` | DateTime | מועד עדכון | אוטומטי |

היבט חשוב:
מוגדר אילוץ ייחודיות על הצירוף `(coordinatorId, programId)` כדי למנוע שמירה כפולה של אותה תוכנית על ידי אותה רכזת.

#### טבלה 5: Inquiry
טבלה זו מייצגת פנייה שנשלחה מרכזת או מגולשת אנונימית למפיקה לגבי תוכנית מסוימת.

| שדה | סוג | מה נשמר בפועל | הערה |
|---|---|---|---|
| `id` | ObjectId | מזהה פנייה | מפתח ראשי |
| `message` | String | תוכן הפנייה | נשמר לאחר סניטיזציה |
| `status` | String | `new` / `read` / `closed` | סטטוס טיפול |
| `contactName` | String | שם הפונה | חובה |
| `contactPhone` | String | טלפון הפונה | חובה |
| `contactEmail` | String? | אימייל הפונה | אופציונלי |
| `contactInstitution` | String? | מוסד הפונה | אופציונלי |
| `coordinatorId` | ObjectId? | שיוך לחשבון רכזת | קיים אם מחוברת |
| `programId` | ObjectId | התוכנית אליה נשלחה הפנייה | חובה |
| `producerId` | ObjectId | המפיקה שקיבלה את הפנייה | חובה |
| `createdAt` | DateTime | תאריך יצירה | אוטומטי |

### סיכום קשרים
1. `Producer` מחזיקה את זהות המשתמשת והתפקיד שלה.
2. `Program` מחזיקה את התוכן העסקי שמוצג באלפון.
3. `Subscription` מחזיקה את מצב המנוי של מפיקה.
4. `SavedProgram` מחזיקה את אזור העבודה האישי של רכזת.
5. `Inquiry` מחזיקה את היסטוריית הפניות בין ביקוש להיצע.

### המלצה לצילום למסמך Word
בפרק זה מומלץ להוסיף:
1. צילום של האוספים מתוך בסיס הנתונים.
2. צילום של `schema.prisma` או תרשים ERD שנבנה ממנו.

<div style="page-break-after: always;"></div>

## חלק טכני: פונקציות ולוגיקה מרכזית
### מבוא
המערכת אינה מבצעת רק פעולות CRUD פשוטות. חלק מהערך שלה נמצא בלוגיקה העסקית שמגינה על התוכן, מוודאת הרשאות, מפרידה בין סוגי משתמשות, ושומרת על עקביות בין מה שנשמר בבסיס הנתונים לבין מה שמוצג בפועל לציבור.

### 1. הרשמה והתחברות
מה קורה בפועל מאחורי הקלעים:
1. המשתמשת ממלאת טופס הרשמה או התחברות.
2. השרת בודק תקינות נתונים ומנרמל את כתובת המייל.
3. בעת הרשמה הסיסמה נשמרת כ-`passwordHash` לאחר הצפנה עם `bcrypt`.
4. בעת התחברות השרת בודק התאמה בין הסיסמה שהוזנה לבין ה-hash השמור.
5. אם הכול תקין, נוצר JWT שמכיל `producerId`, `email`, `role`.
6. הטוקן נשמר בצד הלקוח ומשמש לקריאות API מאובטחות.

### 2. הרשאות לפי תפקיד
במערכת יש שלושה תפקידים: `producer`, `coordinator`, `admin`.

מאחורי הקלעים:
1. נתיבי שרת רגישים משתמשים בבדיקת הרשאה ייעודית.
2. השרת מפענח את ה-JWT ובודק את שדה `role`.
3. רק מפיקה יכולה לנהל תוכניות בדשבורד.
4. רק רכזת יכולה להשתמש בנתיבי שמירה וניהול אזור אישי.
5. רק מנהלת יכולה לאשר, לדחות או למחוק תוכניות דרך פאנל הניהול.

### 3. שליפת אלפון התוכניות
הנתיב הציבורי המרכזי הוא `/api/programs`.

מה קורה מאחורי הקלעים:
1. השרת בונה מערך תנאים דינמי לפי הפרמטרים שהתקבלו מהלקוח.
2. חיפוש חופשי נבדק מול `title` ו-`description`.
3. סינון לפי גיל בודק שהגיל המבוקש נמצא בטווח `minAge <= age <= maxAge`.
4. סינון לפי קהל יעד מתחשב גם בתוכניות שמסומנות `BOTH`.
5. סינון לפי מיקום מתבצע בחיפוש חלקי (`contains`) ולא בהתאמה מדויקת.
6. לאחר השליפה, המערכת מסננת ידנית את הרשומות שמותר להציג לציבור.
7. בפועל מוצגות או תוכניות `approved`, או תוכניות `pending` שיש להן `previousApprovedData`.
8. במקרה של `pending` עם גרסה קודמת, השרת מחזיר לציבור את נתוני הגרסה הקודמת ולא את השינויים החדשים.

### 4. יצירת תוכנית חדשה
בנתיב `/api/dashboard` במתודת `POST`:
1. השרת מוודא שהמשתמשת היא מפיקה.
2. מתבצע אימות לשדות חובה.
3. מתבצעת בדיקה שגיל מינימום וגיל מקסימום תקינים ושמינימום אינו גדול מהמקסימום.
4. הרשומה נשמרת ב-`Program` עם `status = pending`.
5. לאחר השמירה, המערכת מנסה לשלוח מייל אישור קליטה למפיקה.
6. אם שליחת המייל נכשלת, יצירת התוכנית עצמה לא נכשלת.

### 5. עריכת תוכנית קיימת
בנתיב `/api/dashboard` במתודת `PUT`:
1. השרת מוודא שהתוכנית שייכת למפיקה המחוברת.
2. מתבצע אימות מחדש לכל השדות הקריטיים.
3. אם התוכנית הייתה `approved` ועדיין אין `previousApprovedData`, נשמר snapshot של הגרסה המאושרת הקיימת.
4. לאחר מכן התוכנית עצמה מתעדכנת לנתונים החדשים והסטטוס הופך ל-`pending`.
5. כך נוצרת הפרדה בין "מה שממתין לאישור" לבין "מה שעדיין מוצג בחוץ".

### 6. אישור או דחייה של תוכנית
בנתיב `/api/admin/programs` במתודת `PUT`:
1. המנהלת שולחת `programId` ו-`status` חדש.
2. השרת מאמת שהיא אכן מנהלת.
3. אם נבחר `approved`, השרת מעדכן את התוכנית ל-`approved` ומנקה את `previousApprovedData`.
4. אם נבחר `rejected`, השרת מעדכן את התוכנית ל-`rejected` ומנקה את `previousApprovedData`.
5. לאחר הפעולה נשלח מייל מתאים למפיקה.

משמעות עסקית:
1. אישור מפרסם את הגרסה הנוכחית.
2. דחייה מסתירה את התוכנית מהציבור.
3. אין שחזור אוטומטי של הגרסה הקודמת לאחר דחייה.

### 7. פתיחת פרטי תוכנית והגדלת צפיות
כאשר רכזת או אורחת פותחת חלון פרטי תוכנית:
1. מתבצעת קריאה לנתיב הצפיות של התוכנית.
2. השדה `views` ב-`Program` גדל ב-1.
3. הערך נצבר בהמשך לסטטיסטיקות של המפיקה.

### 8. שמירת תוכניות לרכזת
בנתיב `/api/coordinator/saved`:
1. פעולת השמירה עובדת כ-Toggle.
2. אם התוכנית עדיין לא נשמרה, נוצרת רשומת `SavedProgram` חדשה.
3. אם היא כבר שמורה, הלחיצה החוזרת מסירה אותה.
4. האילוץ הייחודי בבסיס הנתונים מגן מפני שמירה כפולה במקרה של שתי לחיצות מהירות.
5. הרכזת יכולה לעדכן `note` ו-`trackStatus` על אותה רשומה.

### 9. שליחת פנייה למפיקה
בנתיב `/api/inquiries` במתודת `POST`:
1. השרת בודק שהתוכנית קיימת ובסטטוס `approved`.
2. הנתונים עוברים אימות בעזרת Zod ופונקציות ולידציה.
3. אם יש טוקן תקין של רכזת מחוברת, הפנייה משויכת לחשבונה.
4. אם אין התחברות, הפנייה נשמרת כפנייה אנונימית עם פרטי הקשר שהוזנו.
5. לאחר שמירת הרשומה ב-`Inquiry`, המערכת מנסה לשלוח מייל למפיקה.
6. השליחה עטופה ב-`try/catch` כדי שכשל חיצוני ב-SMTP לא ימנע שמירה של ליד עסקי חשוב.

### 10. ניהול פניות מצד המפיקה
בנתיב `/api/inquiries`:
1. מתודת `GET` מחזירה למפיקה רק את הפניות שלה.
2. מתודת `PUT` מאפשרת לה לעדכן סטטוס פנייה.
3. העדכון מתבצע עם `updateMany` שכולל גם את מזהה הפנייה וגם את `producerId`.
4. כך בדיקת ההרשאה והעדכון מתבצעים בשאילתה אחת, בצורה יעילה ובטוחה.

### 11. חישוב סטטיסטיקות
בנתיב `/api/dashboard/stats`:
1. נשלפות התוכניות של המפיקה.
2. המערכת מבצעת `groupBy` על טבלת `Inquiry` כדי לחשב כמה פניות יש לכל תוכנית.
3. המערכת מבצעת `groupBy` על טבלת `SavedProgram` כדי לחשב כמה שמירות יש לכל תוכנית.
4. מונה הצפיות נלקח ישירות מהשדה `views` של כל תוכנית.
5. לבסוף נבנה אובייקט סיכום עם סך צפיות, סך פניות, סך שמירות ומספר פניות חדשות.

### 12. אבטחה והגנות
המערכת כוללת מספר שכבות הגנה:
1. `bcrypt` להצפנת סיסמאות.
2. JWT לאימות משתמשות.
3. אימות קלט באמצעות Zod ופונקציות ולידציה.
4. `sanitizeString` לניקוי מחרוזות לפני שמירה.
5. Middleware עם Rate Limiting לנתיבי `/api`.
6. בדיקת `origin` מול `host` להגנת CSRF בבקשות `POST`.
7. כותרות אבטחה כמו `X-Frame-Options` ו-`X-Content-Type-Options`.

### 13. למה הלוגיקה הזו חשובה
הערך הטכני של המערכת אינו רק בהצגת תוכניות, אלא בכך שהיא:
1. מפרידה בין תוכן גולמי לתוכן מאושר.
2. מונעת חשיפת מידע רגיש או גישה לא מורשית.
3. שומרת על יציבות עסקית גם כששירותי צד שלישי, כמו שרת מייל, נכשלים.
4. מאפשרת למשתמשות לעבוד על אותו מידע מזוויות שונות מבלי לאבד עקביות.

<div style="page-break-after: always;"></div>

## חלק טכני: מבנה המערכת (Client / Server / Database)
### מבט על
המערכת בנויה כיישום Full-Stack בפרויקט אחד, על בסיס Next.js. המשמעות היא שגם צד הלקוח וגם צד השרת מנוהלים באותו קוד בסיס, אך עם הפרדה ברורה בין אחריות הממשק, הלוגיקה העסקית והגישה לנתונים.

### 1. צד לקוח (Client)
צד הלקוח אחראי על חוויית המשתמש, איסוף הנתונים מהטפסים, הצגת הרשימות, והפעלת קריאות ה-API.

טכנולוגיות עיקריות:
1. Next.js App Router
2. React
3. TypeScript
4. Tailwind CSS

מה קורה בצד הלקוח:
1. המשתמשת מנווטת בין דפים כמו אלפון, דשבורד, פאנל ניהול ואזור אישי.
2. טפסים אוספים נתונים ונשלחים לשרת כ-JSON.
3. הנתונים החוזרים מהשרת מוצגים כרשימות, כרטיסים, מודאלים וסטטיסטיקות.
4. הטוקן נשמר בדפדפן ומשמש לקריאות מוגנות.

### 2. צד שרת (Server)
צד השרת אחראי על כל הלוגיקה העסקית, בדיקות ההרשאה, השמירה למסד הנתונים, וחיבור לשירותים חיצוניים.

רכיבים עיקריים:
1. Route Handlers תחת `/api/*`
2. ספריות auth ו-api-auth לאימות והרשאות
3. ספריות validation, sanitize, email
4. Prisma Client לתקשורת עם MongoDB

תפקידי השרת:
1. לאמת קלט מהטפסים.
2. לבדוק מי המשתמשת ומה התפקיד שלה.
3. להחליט אילו נתונים מותר להחזיר לכל מסך.
4. ליישם את הזרימה העסקית של אישור/דחייה/פניות/שמירה.
5. לשלוח מיילים ולנהל תקלות בלי להפיל את התהליך העסקי.

### 3. בסיס נתונים (Database)
MongoDB משמש כבסיס נתונים מסמכי, ו-Prisma מספק שכבת עבודה עקבית מעליו.

יתרונות במבנה הנוכחי:
1. קל להחזיק שדות כמו מערכי תמונות וסרטונים.
2. ניתן לשמור אובייקט JSON כמו `previousApprovedData` ללא יצירת טבלה נוספת.
3. Prisma שומר על מודל נתונים קריא ומסודר גם מעל MongoDB.

### 4. שירותים חיצוניים
1. Cloudinary משמש לאחסון וניהול מדיה.
2. Nodemailer משמש לשליחת מיילים.

### 5. זרימת מידע מלאה במערכת
דוגמה לזרימה מקצה לקצה:
1. מפיקה ממלאת טופס הוספת תוכנית בצד הלקוח.
2. הבקשה נשלחת לשרת ב-`POST /api/dashboard`.
3. השרת מאמת הרשאה, מאמת נתונים ושומר ב-MongoDB דרך Prisma.
4. לאחר השמירה השרת מנסה לשלוח מייל.
5. הדשבורד מתעדכן והלקוחה רואה את הסטטוס `pending`.

דוגמה נוספת:
1. רכזת פותחת את אלפון התוכניות.
2. הלקוח שולח פרמטרי חיפוש ל-`GET /api/programs`.
3. השרת בונה פילטרים, שולף נתונים ומפעיל לוגיקת public visibility.
4. רק הנתונים המותרים נשלחים חזרה לממשק.

### 6. אבטחה ברמת המערכת
ה-Middleware פועל כשכבת כניסה לפני חלק ניכר מהנתיבים ומוסיף:
1. הגבלת קצב בקשות.
2. הגנת CSRF.
3. כותרות אבטחה לדפדפן.

### 7. המלצה לצילום למסמך Word
בפרק זה מומלץ לצרף תרשים ארכיטקטורה פשוט, לדוגמה:
`Client -> API Server -> Prisma -> MongoDB`

<div style="page-break-after: always;"></div>

## מדריך משתמש לפי סוגי משתמשים
### אורחת
מה רואה:
1. עמוד בית.
2. אלפון תוכניות.
3. חלון פרטים מלא לכל תוכנית.

מה יכולה לעשות:
1. לחפש ולסנן תוכניות.
2. לראות פרטי קשר.
3. לשלוח פנייה למפיקה.

### רכזת
מה רואה:
1. כל מה שרואה אורחת.
2. אזור אישי עם תוכניות שמורות.
3. סיכום תקציב וסטטוסי מעקב.

מה יכולה לעשות:
1. לשמור ולהסיר תוכניות.
2. לכתוב הערה אישית.
3. לעדכן סטטוס מעקב.
4. לשלוח פנייה מהאלפון ומהאזור האישי.

### מפיקה
מה רואה:
1. דשבורד אישי.
2. סטטוס מנוי.
3. רשימת תוכניות שלה בלבד.
4. עמוד פניות.
5. סטטיסטיקות.

מה יכולה לעשות:
1. להוסיף תוכנית חדשה.
2. לערוך תוכנית קיימת.
3. למחוק תוכנית.
4. לעקוב אחר אישור/דחייה.
5. לעדכן סטטוס פניות.

### מנהלת
מה רואה:
1. פאנל ניהול עם כל התוכניות.
2. סטטוסים ופרטי מפיקה.

מה יכולה לעשות:
1. לאשר תוכנית.
2. לדחות תוכנית.
3. למחוק תוכנית.
4. לפקח על איכות המידע שמגיע לציבור.

<div style="page-break-after: always;"></div>

## תיאור מסכים
### מסך 1: עמוד הבית
מסך פתיחה המציג את מטרת המערכת, ניווט ראשי וכפתורי כניסה לפעולות המרכזיות.

### מסך 2: הרשמה
טופס יצירת חשבון חדש שבו המשתמשת מזינה פרטים אישיים, בוחרת תפקיד רלוונטי, ונשמרת במערכת.

### מסך 3: התחברות
מסך המאפשר כניסה עם אימייל וסיסמה, ולאחר אימות מפנה כל משתמשת לאזור האישי המתאים לתפקידה.

### מסך 4: אלפון תוכניות
רשימת תוכניות עם חיפוש חופשי ופילטרים לפי קטגוריה, גיל, קהל יעד, מיקום ומחיר.

### מסך 5: חלון פרטי תוכנית
מודאל המציג תיאור מלא, מדיה, טווח גילאים, מחיר, פרטי קשר וטופס פנייה.

### מסך 6: אזור אישי לרכזת
מסך המרכז את התוכניות השמורות, ההערות האישיות, סטטוסי המעקב וסיכום התקציב.

### מסך 7: דשבורד מפיקה
מסך ניהול מרכזי שבו המפיקה רואה את התוכניות שלה, מצב המנוי, סטטיסטיקות וסיכומי פעילות.

### מסך 8: טופס הוספה / עריכת תוכנית
טופס מלא להזנת פרטי תוכנית, כולל העלאת מדיה והזנת פרטי קשר ייעודיים לתוכנית.

### מסך 9: הפניות שלי
מסך שבו המפיקה רואה את כל הפניות שהתקבלו, מסננת אותן ומעדכנת סטטוס טיפול.

### מסך 10: פאנל ניהול
מסך המנהלת שבו ניתן לראות את כל התוכניות, לבדוק מי המפיקה, ולאשר, לדחות או למחוק.

### הנחיה לעימוד ב-Word
לכל מסך מומלץ להוסיף:
1. כותרת של המסך.
2. צילום מסך.
3. פסקה קצרה של 3 עד 5 שורות המסבירה מה רואים ומה המשתמשת יכולה לבצע שם.

<div style="page-break-after: always;"></div>

## סיכום
### מה למדתי
1. בניית מערכת Full-Stack אמיתית עם הפרדה ברורה בין ממשק, שרת ובסיס נתונים.
2. מימוש הרשאות מבוססות תפקידים ולא רק התחברות בסיסית.
3. תכנון לוגיקה עסקית מתקדמת סביב workflow של אישור תוכן.
4. שימוש ב-MongoDB וב-Prisma לניהול מודל נתונים מורכב.
5. עבודה עם שירותים חיצוניים כמו Cloudinary ו-Nodemailer.

### קשיים ופתרונות
1. סינון לא מדויק לפי גיל ומיקום נפתר בעזרת פילטרים מחושבים וחיפוש חלקי.
2. שליטה בהרשאות ותפקידים חודדה דרך JWT ובדיקות שרת ייעודיות.
3. בעיית פרסום עדכון לפני אישור נפתרה באמצעות `previousApprovedData`.
4. תלות בשרת מייל חיצוני לא פוגעת עוד בשמירת פניות בזכות `try/catch`.
5. שמירה כפולה של תוכנית על ידי רכזת טופלה באמצעות אילוץ ייחודיות במסד הנתונים.

### מבט קדימה
1. הוספת מנגנון דירוגים וביקורות.
2. הוספת חיפוש חכם והמלצות.
3. הוספת התראות בזמן אמת בתוך האתר.
4. פיתוח אפליקציה לנייד.
5. הרחבת כלי הניהול והדוחות.

</div>
